sha: 3c8595c6954cfad4e6b0dd5d083dd1d74af5a694
branch: recovery/fac-628-flash-drift
task: FAC-628
reviewer: review-fac-628-3c8595c6954c
reviewer-family: openai
builder-family: open-weight
verdict: PASS
reviewed-base: 93d9c2ba42034d18938adcb28d584790adc0dd91
reviewed-head: 3c8595c6954cfad4e6b0dd5d083dd1d74af5a694
---
## Findings and risk

Reviewed the exact range 93d9c2ba42034d18938adcb28d584790adc0dd91..3c8595c6954cfad4e6b0dd5d083dd1d74af5a694 (four linear commits, 10 files). `herd review-classify recovery/fac-628-flash-drift --tier R3 --pin 3c8595c6954cfad4e6b0dd5d083dd1d74af5a694 --json` independently returned an R3 floor, with core-orchestration paths and required deterministic, different-family, security-capable, and explicit high-risk gates.

The candidate satisfies the acceptance criteria. `standing.ForbiddenLaunchModel` rejects the resolved forbidden Grok route before side effects; `validateDecisionBeforeSideEffect` protects the pre-tab path and `startStandingAgent` protects the final pre-start/provenance seam used by `herd up` and standing relaunch. `spinModelDrift` requires accepted, timestamped receipt evidence for the matching tab/pane/worktree and a resolved live argv model, names recorded and live values, and leaves incomplete identity unknown. `emitSpin` keeps JSON pure and carries drift structurally. `moved` suppresses continuation/state-change churn while preserving artifact progress, and unreadable goal-guard counters are explicitly unavailable rather than zero.

1. No blocking, high, medium, or nit findings for this exact revision.

Independent non-vacuity controls were meaningful: removing the latest launch guard made both public forbidden-model tests fail; removing the continuation guards made `TestEmptyContinuationLoopIsNotProgress` fail on the behavioral assertion (`PROGRESSING` with state-change evidence). Both mutations were restored and the leased worktree is clean.

Residual risk: the packet forbids live provider/pane mutation, so the stated live-lane verification was not independently executed. Clean WSL CI and any managed coordinator receipt remain coordinator-owned gates; neither is represented here as passed. The local graph was indexed at the reviewed head (322 nodes, 5,159 edges, 3 Go files).

Patch identity supplied by the packet: patch_id f354187e6a7bef720c525cb58ae3f816d02749597; verification_digest sha256:1ba49613e923f1cbddf273d26003e698de77eb207d40eab8beae1834d8dee6ae.

## Tests run

- `go version` -> `go version go1.26.6 darwin/arm64`.
- `go test -count=1 ./pkg/standing/ ./pkg/spin/ ./cmd/herd/` -> exit 0 (`pkg/standing` 5.165s, `pkg/spin` 0.408s, `cmd/herd` 314.856s).
- `go test -race -count=1 ./pkg/spin/ ./pkg/standing/` -> exit 0.
- `go build ./...` -> exit 0.
- `go vet ./pkg/spin/ ./pkg/standing/ ./cmd/herd/` -> exit 0.
- `go run ./scripts/hermeticity/ pkg/spin pkg/standing cmd/herd` -> exit 0.
- `git diff --check 93d9c2ba42034d18938adcb28d584790adc0dd91..3c8595c6954cfad4e6b0dd5d083dd1d74af5a694` -> exit 0.
- Parent-blob mutation of `cmd/herd/main.go` (50a657c9d74300270f807abf496eb4daa56c7560) with `go test -count=1 ./cmd/herd -run Test\(ValidateDecisionBeforeSideEffectRefusesForbiddenModel\|StartStandingAgentRefusesForbiddenModelBeforeStart\)` -> expected exit 1; both assertions failed, then the candidate file/index were restored.
- Compiling mutation removing both continuation-churn guards with `go test -count=1 ./pkg/spin -run '^TestEmptyContinuationLoopIsNotProgress$'` -> expected exit 1 on the shipped behavioral assertion, then restored.
