sha: 572cd718f40be5f1dbd8e5fca252ab2623e5df77
branch: recovery/fac-740-flash-retention
task: FAC-740
reviewer: review-fac-740-572cd718f40b
reviewer-family: openai
builder-family: open-weight
verdict: FAIL
reviewed-base: 93d9c2ba42034d18938adcb28d584790adc0dd91
reviewed-head: 572cd718f40be5f1dbd8e5fca252ab2623e5df77
---

## Review scope

Reviewed the exact candidate range 93d9c2ba42034d18938adcb28d584790adc0dd91..572cd718f40be5f1dbd8e5fca252ab2623e5df77 in the leased pool surface. This is an R3 review of the full 11-file, 3-commit range. The prior FAIL for candidate 3309f85d2da0 is historical and is not reassessed here.

## Acceptance

The candidate improves retention substantially: the new verdict artifact path compares exact bytes, recovery requires signed record/effect evidence, strict JSON EOF decoding is shared by signing and recovery, and DedupeID-bearing delivered callbacks are no longer swallowed by the identity-less completion fast path. The focused tests and mutation controls confirm those protections are active.

## Findings and risk

1. [High] An unsigned mailbox marker is still accepted as proof of provider delivery. At `cmd/herd/main.go:9587-9597`, `HasDeliveredVerdict(effectID)` short-circuits the broker before provider `ListComments` readback. `pkg/mail/callback.go:275-299` matches only the exact DedupeID; it does not authenticate the callback or validate its bound repository, task, candidate, base, lease, verdict, canonical effect, or provider evidence. A local mailbox writer can post a callback with the expected `verdict-delivered:` DedupeID while the provider has no canonical verdict. The broker then returns success and `retainVerdictInboxArtifact` signs and retains a locally composed artifact, which proves only the broker's current inputs—not delivery to the provider. This violates the packet acceptance requirement that unsigned callbacks fail closed and allows a false consumable-delivery state. Require provider-side readback, or an independently authenticated and fully bound delivery receipt, before this short-circuit can return success or retain consumable evidence.

## Residual risks

The legacy `RetainArtifact` path still uses a 16-hex-character digest prefix for its filename and collision check in `pkg/reviewingest/retain.go:243-261`; this predates the new exact-byte `RetainVerdictArtifact` path and should be scoped separately if all review-ingest retention is intended to receive the same collision guarantee. The same-byte collision branch in the shared publisher also does not re-check file mode after the initial existence check. These were not used to change the verdict because the unsigned-delivery marker is a direct R3 acceptance failure.

patch_id: 70465cba7ffddb4eace546295f94599eaf5e0e10
verification_digest: sha256:907af5d94ca358591d21461c04a608cf1c660b647a047ef8f5b72c9e283accf6

## Tests run

- `go version` — exit 0 (`go1.26.6 darwin/arm64`).
- `code-review-graph update --repo .` and graph status/detect-changes review — exit 0; exact candidate indexed, 51 changed functions/classes analyzed, risk score 0.55.
- `git diff --check 93d9c2ba42034d18938adcb28d584790adc0dd91..572cd718f40be5f1dbd8e5fca252ab2623e5df77` — exit 0.
- `GOMAXPROCS=2 go test -count=1 -race ./pkg/mail -run '^(TestPostAndDrainCallbacks|TestDrainCallbacks_EmptyInbox|TestPostCallback_VerdictMarkerNotSwallowedByCompletion)$'` — exit 0.
- `GOMAXPROCS=2 go test -count=1 -race ./pkg/reviewingest` — exit 0.
- `GOMAXPROCS=2 GOFLAGS=-p=1 go test -count=1 -race ./cmd/herd -run '^(TestBrokerVerdict_RetainsCanonicalInboxArtifact|TestBrokerVerdict_RetryAfterArtifactDelete_HealsCrashWindow|TestBrokerVerdict_ClaimedButUndelivered_RetryNeverReportsSuccess|TestBrokerVerdict_RetentionConflictThenRetry_Reconciles|TestRecoverVerdictMaterializesDeliveredEffectsForAdmission|TestRecoverVerdictRefusesIntentOnlyAndSupersededEvidence|TestVerdict_TwoBrokersDeliverExactlyOnce|TestSignAndRecoveryRefuseTrailingData)$'` — exit 0.
- `GOMAXPROCS=2 GOFLAGS=-p=1 go test -count=1 ./pkg/reviewingest ./pkg/reviewledger ./pkg/reviewsup` — exit 0.
- `GOMAXPROCS=2 GOFLAGS=-p=1 go build ./...` — exit 0.
- `GOMAXPROCS=2 GOFLAGS=-p=1 go vet ./pkg/reviewingest ./pkg/mail ./cmd/herd` — exit 0.
- `GOMAXPROCS=2 GOFLAGS=-p=1 go run ./scripts/hermeticity/` — exit 0.
- Temporary callback regression mutation (`if cb.DedupeID == ""` bypassed) with `GOMAXPROCS=2 go test -count=1 -race ./pkg/mail -run '^TestPostCallback_VerdictMarkerNotSwallowedByCompletion$'` — expected exit 1; the test reported the verdict-delivered post was swallowed. Restored and reran the test — exit 0.
- Temporary repository-identity regression mutation with `GOMAXPROCS=2 go test -count=1 ./pkg/reviewingest -run '^TestRetainVerdictArtifact_RefusesRepoIdentityMismatch$'` — expected exit 1; the test reported the mismatched record was accepted. Restored and reran the test — exit 0.
- Broad `go test -race ./pkg/reviewingest ./pkg/mail` and an initial broker invocation were interrupted after host-level high-contention stalls in existing nested builds/mail writer contention; they are not treated as candidate failures. The packet-owned clean WSL `make ci` and coordinator-managed verification were not run by this reviewer.

## Verdict

FAIL — the unsigned delivered-marker short circuit must be corrected before merge.
