sha: 47d0d532739bc51e9258d806185ed2cd3a985bc3
branch: fac-642-bin-parity
task: FAC-642
reviewer: grok
reviewer-family: xai
builder-family: openai
verdict: PASS
reviewed-head: 47d0d532739bc51e9258d806185ed2cd3a985bc3
---

# FAC-642 review — 47d0d532739bc51e9258d806185ed2cd3a985bc3

Isolation: cwd and `git rev-parse --show-toplevel` are the leased pool worktree
(pool-fanout-3/pool-01). Surface symlink resolves to that same tree. Detached
HEAD was pinned at the candidate after the slot had been sitting on origin/main.
`docs/prompts/review-contract.md` is absent on this SHA; contract used was
`.herd/prompts/reviewer.md` plus `.herd/prompts/review-verdict.template.md`.

## Change

Single commit on `fac-642-bin-parity` over merge-base `f2ca0d320`.
Makefile `bin-parity` now always runs `go run ./scripts/binparity`. The
command resolves a missing `CHAINSEER_BIN` via `gitroot.ProjectRoot` as the
sibling `chainseer/bin`, classifies `SOURCE_UNAVAILABLE` (exit 2),
`PARITY_MISMATCH` (exit 3), and `MANIFEST_INVALID` (exit 4), and skips only
when `CHAINSEER_PARITY_OPTIONAL=1`. The provenance snapshot is rebaselined
from 124 entries at `ad284cc5` to 51 entries at `882da80` (50 product
exemptions + `bin/herdforge` deprecated wrapper). Dropping the 52
`herdforge_command_replacement` and 21 `generic_capability` rows is required
for an inventory match once those executables left Chainseer; extras would
still fail closed.

## Graph

`code-review-graph` full build at this SHA: 1395 files, 16476 nodes.
`detect-changes --base f2ca0d320` risk 0.65, 4 files, 0 affected flows.
Graph `tests_for` edges on `scripts/binparity/main.go` are empty; command-level
tests still exercise `defaultSourcePath`, `sourceDirectoryAvailable`,
`reportSourceUnavailable`, and `auditSource` through the compiled binary.
`pkg/gitroot.ProjectRoot` is worktree-invariant (common-dir parent), so a
pool/worktree run still looks next to the canonical Herdforge checkout, not
next to the slot. That matches the sibling layout the tests construct.

## Verification

- `go test -count=1 ./scripts/binparity/` green (0.410s). `go vet` clean.
- Non-vacuity in-pool: swapped `scripts/binparity/main.go` for the parent
  blob; tests failed to compile (`undefined: exitSourceUnavailable` /
  `exitParityMismatch`). Restored; `git status --porcelain` clean;
  HEAD still 47d0d532739bc51e9258d806185ed2cd3a985bc3.
- Committed JSON against a matching /tmp source: `binparity: PASS: 51
  executable dispositions`. Extra file → `PARITY_MISMATCH` exit 3; missing
  file → `PARITY_MISMATCH` exit 3. Compiled binary `SOURCE_UNAVAILABLE` is
  exit 2.

[P3] Update the handoff doc's stale 124-file coverage claim — docs/architecture/CHAINSEER-HERDFORGE-HANDOFF.md:22

The snapshot this commit ships has 51 entries, but the operational handoff
doc still says the manifest covers 124 executables. Operators reading that
file will think the gate still tracks the retired control-plane scripts.

## Residual risk

- Live sibling Chainseer HEAD `d8f55210` currently has 49 executables; default
  `make bin-parity` here reports `PARITY_MISMATCH` (49 vs 51), missing
  `bin/check-lasso-runtime` and `bin/check-runtime-pressure`. That is the gate
  working against an unpinned HEAD, not a logic bug in this SHA. CI still
  checks out Kampe/chainseer at depth 1 (not `882da80`).
- `go run` (Makefile/`make lint`) collapses the program's 2/3/4 to the go
  tool's exit 1 plus `exit status N` on stderr. Class labels remain. Tests
  correctly assert numeric codes on the compiled binary.
- No command-level coverage for `MANIFEST_INVALID` (exit 4).
- `cleanCommandEnv` drops `CHAINSEER_*` and `HERD_*` but not `GIT_DIR` /
  `GIT_WORK_TREE`; host GIT_DIR was unset in this review, so it was not
  exercised.

PASS: no blocking defect on this exact revision. Cross-family check holds
(openai builder, xai reviewer). Risk is R1 (lint-gate script + snapshot).
