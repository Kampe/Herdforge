sha: 495ca7a5fcd12ed7babdafbaffd54c5c15a72e8b
branch: repair/fac-765-authenticated-host-ingest
task: FAC-765
reviewer: review-fac-765-495ca7a5fcd1
reviewer-family: openai
verdict: FAIL
reviewed-base: 9737258c9ecd7a48da215ea729cceff3cbc1d011
reviewed-head: 495ca7a5fcd12ed7babdafbaffd54c5c15a72e8b
---
## Findings and risk

Reviewed the complete eight-commit production range
`9737258c9ecd7a48da215ea729cceff3cbc1d011..495ca7a5fcd12ed7babdafbaffd54c5c15a72e8b`
as R3. The graph reported 29 changed files, 21 changed functions/classes, and
seven test gaps. The required canonical-membership, StartToken, builder-only
refusal, positive membership, append-only reassessment/idempotence, dissent,
HostIngest order, reachable `pkg/review.Vetoed`/pulse, and deterministic
candidate-display paths were exercised with focused checks. Those checks pass,
but the following authentication defect blocks approval.

1. **Blocker — canonical launch provenance is replayable across candidates and
   tasks.** `cmd/herd/review_host_ingest.go:90-120` calls
   `AcceptedCanonicalMember` only as a locator-membership check and discards
   the returned canonical receipt. `pkg/launch/authority.go:466-468`'s
   `sameReceipt` identity omits `CandidateSHA`, and
   `pkg/launch/receipts_canonical.go:66-92` selects an accepted review launch
   by reviewer name globally, without binding it to the requested candidate,
   task, repository, or lane. `ReachingBuilderReceipt` also proves only that a
   canonical accepted branch reaches the requested SHA; it does not require
   the receipt's own `CandidateSHA` to equal that SHA. The resolver then labels
   the resulting host/session proof with the requested SHA, creating a false
   binding.

   A temporary same-package regression proof placed an accepted builder and
   reviewer launch for `candidate-a` in the canonical log, requested
   `candidate-b`, and supplied a reaching branch callback. The proof expected
   refusal but the resolver returned authenticated provenance (`go test` exit
   1); the temporary file was deleted afterward. This permits a canonical
   receipt/host from another candidate or task to authenticate HostIngest for
   the requested candidate, undermining the receipt-file locator-only and
   actual reviewer host/session requirements and potentially admitting a
   cross-identity verdict.

   Required correction: bind every canonical accepted member, reviewer launch,
   and builder proof used by HostIngest to the requested task/repository/lane
   and candidate SHA, and add a regression test that rejects cross-candidate
   and cross-task replay while retaining the existing StartToken and host/
   session mismatch refusals.

   The prior retained `a334` PASS, `3a` FAIL, and `52bb` FAIL remain historical
   evidence. The retained `52bb` W4 conflict-fixture failure was not claimed as
   passing. The requested `f519c1db3cb5b33ec4f60a2930db04b4821e2e1064a69bff1f3c68d8ca2b2036`
   artifact was not present in this pool or repository checkout, so it was not
   modified. No builder family is asserted here; primary canonical ingest must
   derive it from the accepted launch record.

## Tests run

All Go commands below used
`$HOME/.local/share/mise/installs/go/1.26.4/bin/go` and ran in the registered
pool worktree. Exit statuses are the observed statuses.

- `go test ./pkg/launch -run 'TestAcceptedCanonical|TestReachingBuilder|TestAuthorityCrashRestart|TestAuthoritySamePacketActiveBindingMismatch|TestAuthorityHasStartedRejectsEveryExactBindingMismatch' -count=1` — exit 0.
- `go test ./cmd/herd -run 'Test(ParseReviewHostIngest|ForgedReceipt|CanonicalReviewLaunch|HostIngestRefuses|LedgerReviews|LoadReapEvidence|TakeCLIFlag|ReachableFromBranch|LastPassSelection|LatestWithinBranch|HostLabelledHarvest|HarvestCommitsCandidateRange|ResolveHarvestCandidatePins)' -count=1` — exit 0.
- `go test ./pkg/reviewledger -run 'Test(HostIngest|AdmitReduced|Veto|MergeReadiness|CompleteAdmission|BindEvidence|SameReviewerReassessment|Reassessment|Eligible|Queued)' -count=1` — exit 0.
- `go test ./pkg/review -run 'TestVetoedKeepsCrossHostDissent' -count=1` — exit 0.
- `go test ./pkg/mergeadmit -run 'TestReconcileLandedReducedRefusesAuthenticatedCrossHostDissent' -count=1` — exit 0.
- Mutation control: replacing `pkg/reviewledger/admission.go` with its parent revision and running `go test ./pkg/reviewledger -run TestAdmitReducedRefusesAuthenticatedCrossHostDissent -count=1` — exit 1 as required; the mutation admitted 81–95 of 100 iterations in four cases.
- Mutation control: replacing `cmd/herd/candidate_cmd.go` with the pre-coherent-display revision and running `go test ./cmd/herd -run TestLedgerReviewsConflictingHostVetoesAreStable -count=1` — exit 1 as required; output selected `b.md/xai` instead of deterministic `a.md/anthropic`.
- Mutation control: replacing paired `pkg/review/ledger.go` and `pkg/review/pipeline.go` with the pre-dissent revision and running `go test ./pkg/review -run TestVetoedKeepsCrossHostDissent -count=1` — exit 1 as required; both cross-host FAIL/PASS and BLOCKED/PASS cases lost `Vetoed`.
- Temporary cross-candidate provenance proof: `go test ./cmd/herd -run TestTEMPCanonicalReviewProvenanceRejectsDifferentCandidate -count=1` — exit 1, exposing the blocker above; the temporary test was removed and the worktree restored clean.
- `go vet ./cmd/herd ./pkg/launch ./pkg/review ./pkg/reviewledger ./pkg/mergeadmit` — exit 0.
- Retained earlier broad check, not blindly repeated: `go test ./cmd/herd ./pkg/reviewledger ./pkg/review ./pkg/mergeadmit` — exit 1. Failures included `cmd/herd/TestObserveVerifyLandedSquashPreservesCandidate`, `pkg/review/TestPipelineContract_ConflictEvidenceIsRebaseNeeded`, and multiple `pkg/mergeadmit` equivalent/squash fixture tests. These are preserved as the known W4 conflict-fixture failures.
- Retained earlier `make lint all`, not repeated — exit 2 at the security gate with `jq: Cannot iterate over null`.
- Retained earlier `make all`, not repeated — exit 2 with failures in the cmd/herd squash fixture, harness hook timeout/unavailable checks, herdr fence, launch hook timeout, mergeadmit squash, review conflict fixture, security PATH secret-material check, sync force-branch check, verifier hermetic Go availability, and scripts/binparity manifest checks. No full-suite pass is claimed.

The pool worktree was clean after the temporary proof and bounded checks; no
source, ledger, board, host-ingest, product-push, merge, or Done mutation was
performed.

## Author instructions

Reject the candidate until the canonical accepted launch proof is bound to the
requested task/repository/lane and candidate SHA for both reviewer and builder
receipts. Add cross-candidate and cross-task replay refusals, then rerun the
focused HostIngest/admission/dissent/display checks and the relevant mutation
controls. Preserve the historical retained failures and do not convert the
known conflict-fixture failure into a pass.
