sha: 3a436b5228e09e1eeb43fe18bdb9a894f0946df4
branch: repair/fac-765-authenticated-host-ingest
task: FAC-765
reviewer: review-fac-765-3a436b5228e0
reviewer-family: openai
verdict: FAIL
reviewed-base: 9737258c9ecd7a48da215ea729cceff3cbc1d011
reviewed-head: 3a436b5228e09e1eeb43fe18bdb9a894f0946df4
---
## Findings and risk

Reviewed exactly `9737258c9ecd7a48da215ea729cceff3cbc1d011..3a436b5228e09e1eeb43fe18bdb9a894f0946df4` as one R3 production range (five commits). The canonical membership, caller-locator-only, reviewer-role/host/session, builder-only refusal, StartToken tamper refusal, positive membership, append-only preservation, and shared CLI flag checks are implemented and their focused tests pass. The range remains not mergeable because the host-aware append path creates a dissent-hiding merge-gate state.

1. **[Blocker] Cross-host dissent is discarded by the eligibility projections.** `pkg/reviewledger/host_ingest.go:242-251` intentionally treats `(SHA, reviewer, authenticated host)` as distinct and appends a second verdict for another host. However, `pkg/reviewledger/operations.go:777-785` and `:808-829` still index verdicts and launch rows only as `SHA:reviewer`. A FAIL/BLOCKED from host A followed by a PASS from host B with the same reviewer name overwrites the veto in `latest`, so `Eligible` can return `true` and harvesting can proceed despite recorded dissent. A temporary pool-only regression test produced `eligible=true err=nil` and exited 1; it was removed and the pool is clean. Preserve every authenticated host verdict in the dissent/latest projections and make all related queue/eligibility keys consistent with the host-aware identity, while retaining SHA-level veto behavior.

This is an R3 merge/readiness integrity failure, not a cosmetic or test-only concern. The existing append-only bytes remain intact, but the consumer that authorizes harvest no longer reflects those bytes. The correction must also retain same-host idempotence and the existing cross-family checks.

## Tests run

- `code-review-graph build --repo .` — PASS; graph indexed 17,640 nodes / 225,423 edges at the reviewed head. `detect-changes --base 9737258c9ecd7a48da215ea729cceff3cbc1d011 --brief` — 17 files, overall risk 0.85.
- `/home/kampe/.local/share/mise/installs/go/1.26.4/bin/go test ./pkg/launch -run 'TestAcceptedCanonicalMember|TestAcceptedReviewLaunchFor|TestReachingBuilderReceipt' -count=1` — PASS (exit 0).
- `/home/kampe/.local/share/mise/installs/go/1.26.4/bin/go test ./pkg/reviewledger -run 'TestHostIngest|TestReconcileBuilderFamily|TestCompleteLaunchProvenance' -count=1` — PASS (exit 0).
- `/home/kampe/.local/share/mise/installs/go/1.26.4/bin/go test ./cmd/herd -run 'Test(ParseReviewHostIngest|ForgedReceipt|CanonicalReviewLaunch|HostIngestRefuses|TakeCLIFlag|ParseReviewBindEvidenceArgs|HostLabelledHarvest|LastPassSelection|LatestWithinBranch|HarvestCommitsCandidateRange|ResolveHarvestCandidatePins)' -count=1` — PASS (exit 0).
- Mutation control: disabled `!p.Member`; `TestHostIngestRefusesNonmemberProvenance` failed as required (exit 1). Restored.
- Mutation control: disabled the `ReviewerRole` filter; `TestAcceptedReviewLaunchForRejectsBuilderIdentity` failed as required (exit 1). Restored.
- Mutation control: disabled the receipt `CreatedAt.After(commitTime)` refusal; `TestHostIngestRefusesMismatchUnreachableAndFlagOnlyTrust` failed as required (exit 1). Restored.
- Temporary cross-host dissent regression (FAIL host A, PASS host B, same reviewer) failed with the defect (`eligible=true err=nil`, exit 1); temporary test removed.
- `/home/kampe/.local/share/mise/installs/go/1.26.4/bin/go test ./pkg/launch ./pkg/reviewledger -count=1` — FAIL (exit 1) in unrelated hook-timeout-sensitive `pkg/launch` tests: `TestOrdinaryRequestCannotBypassProductionDiscovery`, `TestClaudeCommandIncidentRequiresBoundHealthPolicyBeforeEffects`, and `TestValidateOptionalHookWarningIsDeduplicatedAndIdentityPreserved`; `pkg/reviewledger` passed. No blind retry was made.
- `/home/kampe/.local/share/mise/installs/go/1.26.4/bin/go test -race ./pkg/launch ./pkg/reviewledger -count=1` — same unrelated `pkg/launch` hook-timeout failures (exit 1); `pkg/reviewledger` passed.
- `/home/kampe/.local/share/mise/installs/go/1.26.4/bin/go vet ./pkg/launch ./pkg/reviewledger ./cmd/herd` — PASS (exit 0).
- `git diff --check 9737258c9ecd7a48da215ea729cceff3cbc1d011 HEAD` and `gofmt -d` over changed Go files — PASS (exit 0). `git status --porcelain` — clean after restoring controls.

## Residual risk

Builder family was not asserted in this W4 artifact because the primary canonical launch log is unavailable locally; native ingest must derive it from the accepted `fac-765-builder` receipt. The absent repository `docs/prompts/review-contract.md` was noted; the tracked `.herd/prompts/reviewer.md` and `.herd/prompts/review-verdict.template.md` were read. No product files, ledger, board, host-ingest, merge, or push operations were performed.
