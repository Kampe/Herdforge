sha: 2a3a20d57ba7e17f923d0260ed60edfff3fe27f9
branch: fix/fac-652-direct
task: FAC-652
reviewer: review-fac-652-2a3a20d57ba7
reviewer-family: xai
builder-family: unrecorded
verdict: PASS
reviewed-head: 2a3a20d57ba7e17f923d0260ed60edfff3fe27f9
---

# FAC-652 review evidence

task_ref: FAC-652
candidate_sha: 2a3a20d57ba7e17f923d0260ed60edfff3fe27f9
base_sha: a14ab3f0386265f1d97e17096500c4bfe19b13bf
branch: fix/fac-652-direct
risk_tier: R2 (receipt-gated board close / coordinator mint authority; bounded to approveOne)
author_family: unrecorded (prefilled launch provenance; not re-derived)
reviewer_family: xai
reviewed_at: 2026-09-07
docs/prompts/review-contract.md is absent in this tree; review followed .herd/prompts/reviewer.md and .herd/prompts/review-verdict.template.md.

Isolation: cwd and `git rev-parse --show-toplevel` both resolve to the leased pool worktree under `.herd/pool/pool-01`. Surface symlink `.herd/review-surfaces/review-fac-652-2a3a20d57ba7` -> `../pool/pool-01`. HEAD == candidate. Working tree clean after the vacuity swap was restored.

## Diff

Two commits on `fix/fac-652-direct` vs merge-base `origin/main`:

- `81b17a56` fix: bind receipt approval to coordinator mint identity (FAC-652)
- `2a3a20d57ba7` test: cover receipt board-done broker replay FAC-652

Changed paths: `cmd/herd/main.go` (+11), `cmd/herd/review_cli_test.go` (+37 / -26 net vs first test).

Production change: after `AcquireLease` + `AdvanceFence` on the receipt path in `approveOne`, if the unwrapped provider is `*provider.KaneoProvider` and `stack.Minter != nil`, attach that minter fail-closed and put `provider.WithMintIdentity` on the call ctx from the acquired lease (`Repo/Provider/Project/TaskRef/OwnerID`) before `hsync.BoardDoneFenced`. This matches `fencedBoardDone` in `cmd/herd/fac147_fence.go` and the guarded mutate helpers in `pkg/provider/claim_stack.go`. Identity stays on ctx (immutable per call); `KaneoProvider.minter` is the process-owned coordinator minter, not per-task fields.

`runBoardDone` already delegates to `approveOne`, so both CLI entrypoints share the bind. Override close already went through `fencedBoardDone` and is out of this slice.

`mutateStatus` / `AddComment` in `pkg/provider/kaneo_fence.go` fail closed with `coordinator mint requires WithMintIdentity on ctx` when a minter is attached and ctx identity is missing. That is the exact production refusal this bind repairs.

## Tests

`TestApproveBroker` drives the stock in-process coordinator broker (`HERD_FENCE_COORDINATOR=1`, `HERD_FENCE_ATOMIC_SERVER` stripped) for `approve` and `board-done`. The board-done case starts the fake card in `in-progress` (FAC-756 recovery shape). Both require exactly one provider status mutation and a replay that does not increment `fk.patches`.

Non-vacuity (pool-local swap of `cmd/herd/main.go` to parent blob `a14ab3f0`, then restore):

```
go test ./cmd/herd -count=1 -timeout 180s -run 'TestApproveBroker$'
```

RED without the bind:

- approve: `UpdateStatus: kaneo: coordinator mint requires WithMintIdentity on ctx (immutable per call)`
- board-done: same refusal

GREEN on the candidate (same command plus `TestApproveCLI_BoundMutationAndPassCallback`): `ok github.com/Kampe/Herdforge/cmd/herd 4.835s`

Graph (`code-review-graph` full build at this SHA: 1451 files / 17103 nodes): `WithMintIdentity` callers are `approveOne`, `fencedBoardDone`, and `ClaimStack.Mutate{Status,Claim,Comment}Guarded`. `approveOne` callers: `runApprove`, `runBoardDone`, `runForgeE`. Graph `tests_for approveOne` is empty because coverage is a subprocess CLI test, not a direct unit call; that is a graph blind spot, not a missing gate.

## Findings

No findings.

## Residual risk

- builder-family is the prefilled launch value `unrecorded`; independence vs xai holds as unequal strings. If ingest treats `unrecorded` like `unproven`, this PASS may need hand admission.
- Mint identity `TaskRef` is the review lease (`reviewLeaseTaskRef` → `fac-1:review`), not the board card ref. That is the same key `approveOne` acquires and is consistent with existing fence helpers.
- Comment mint after the status write can still be swallowed by pre-existing `BoardDoneFenced` non-timeout comment handling; not introduced here, and the same ctx identity covers both mints when they run.
