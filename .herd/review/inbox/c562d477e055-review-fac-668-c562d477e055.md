sha: c562d477e055a4cedf5571cf2ccbc8e47dbf322d
branch: harvest/fac668-contract-887
task: FAC-668
reviewer: review-fac-668-c562d477e055
reviewer-family: openai
builder-family: open-weight
verdict: FAIL
reviewed-base: 93d9c2ba42034d18938adcb28d584790adc0dd91
reviewed-head: c562d477e055a4cedf5571cf2ccbc8e47dbf322d
---
## Findings and risk

Reviewed the complete range 93d9c2ba42034d18938adcb28d584790adc0dd91..c562d477e055a4cedf5571cf2ccbc8e47dbf322d (all three integration commits), with FAC-668 as the task identity. Risk floor: R3 because this changes fail-closed review admission and can evict or reset live reviewer pool state. Acceptance criteria assessed: candidate-owned reviewer/template paths; exact-SHA surface validation; missing/stale contract refusal; validation before packet/pane side effects; provisional cleanup; unchanged `init --full` output and no baseline regeneration.

1. [Blocker] `runPoolReview` validates the candidate contract too late. In `cmd/herd/review_pool.go`, the pool lease is acquired at lines 247-267, stale occupants may be evicted at lines 268-291, the leased worktree is reset at lines 292-294, and the review-surface symlink is created at lines 296-337. Only then do lines 342-346 call `verifySurfaceCandidate` and `verifyReviewContract`. A missing or invalid contract therefore fails closed only after the command has already changed pool state and may have terminated an unrelated reviewer. The defer releases the lease, but it does not restore the reset worktree, revive evicted occupants, or remove the provisional surface. Move candidate contract validation ahead of pool lease/eviction/reset, and make any later provisional surface cleanup explicit.

2. [High] `verifyReviewContract` checks `os.Stat` and `IsRegular`, not candidate provenance. `git reset --hard` does not remove untracked files, so a stale `.herd/prompts/reviewer.md` or `.herd/prompts/review-verdict.template.md` left by a prior occupant can satisfy this gate even when the exact candidate commit does not contain that path. `os.Stat` also follows symlinks, allowing a candidate-surface path to resolve to a regular file outside the candidate worktree. The new missing-contract guarantee can therefore accept instructions/template content that is not owned by the reviewed SHA. Validate the paths against the candidate tree (and reject symlinked/non-regular entries) before admission.

The direct helper guards are non-vacuous, but the added source-order test only proves the contract check precedes packet writing and retains the lease-release defer; it does not prove the required pre-pool ordering or cleanup of reset/evicted state. Residual risk remains around the untested public `runPoolReview` failure path because exercising it would mutate live pool/provider state, which this review was prohibited from doing.

## Tests run

- `code-review-graph status` — exit 0; graph had 299 nodes/5001 edges and was built at the reviewed head.
- `code-review-graph detect-changes --base 93d9c2ba42034d18938adcb28d584790adc0dd91 --brief` — exit 0; identified 4 changed files, `runInitFull` as an untested changed function, and overall risk 0.35.
- `go test ./cmd/herd -run 'Test(ReviewPacketNamesRepositoryOwnedContractPaths|ReviewContractRequiresBothFilesOnCandidateSurface|VerifySurfaceCandidateRefusesAStaleAlias|VerifySurfaceCandidateAcceptsAMatchingSurface|VerifySurfaceCandidateGuardsBothReadyAndLaunch|ReviewContractGuardPrecedesPacketAndKeepsPoolCleanup|NoLaunchDoesNotReleaseItsLease)$' -count=1` — exit 0.
- Mutation control: temporarily replaced `verifyReviewContract` with a compiling no-op; `go test ./cmd/herd -run '^TestReviewContractRequiresBothFilesOnCandidateSurface$' -count=1` — exit 1 (expected RED); restored the function.
- Mutation control: temporarily replaced `verifySurfaceCandidate` with a compiling no-op; `go test ./cmd/herd -run '^TestVerifySurfaceCandidateRefusesAStaleAlias$' -count=1` — exit 1 (expected RED); restored the function.
- `go test ./cmd/herd -count=1` — exit 0 (328.399s).
- `go test -race ./cmd/herd -count=1` — exit 0 (340.274s).
- `go build ./cmd/herd` — exit 0.
- `go vet ./cmd/herd` — exit 0.
- `go run ./scripts/hermeticity/` — exit 0.
- Candidate binary `init --full` smoke test in an isolated temporary directory — exit 0; output retained the existing scaffold text and generated no verdict template/baseline artifact. Base and head both tracked the reviewer contract and verdict template.
- `git diff --check` — exit 0; tracked candidate files were restored after mutation controls. An unrelated untracked `cmd/herd/.herd/toolprobe-cache.json` appeared during the run and was preserved rather than deleted.
