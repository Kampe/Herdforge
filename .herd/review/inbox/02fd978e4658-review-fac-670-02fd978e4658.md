sha: 02fd978e46581cb6d89e3fcb84502db1da52785a
branch: herd/fac-670
task: FAC-670
reviewer: review-fac-670-02fd978e4658
reviewer-family: xai
builder-family: unrecorded
verdict: PASS
reviewed-head: 02fd978e46581cb6d89e3fcb84502db1da52785a
---

Risk R2. Isolated pool-02 worktree; `git rev-parse --show-toplevel` resolves under `.herd/pool/`, HEAD equals the candidate SHA, working tree clean after mutation restore. Packet path `docs/prompts/review-contract.md` is absent in this tree; review used `.herd/prompts/reviewer.md`, `.herd/prompts/review-verdict.template.md`, and `.herd/prompts/routing.md`. builder-family is the prefilled launch value `unrecorded` (not re-derived). This review was therefore independent of a recorded author family; ingest may still need hand admission for unrecorded provenance.

## Scope vs FAC-670

The card requires a non-TTY Grok probe that returns the exact sentinel, flat-rate billing that is not rendered as 0/0 or fabricated headroom, HTTP/auth/decode failures remaining UNKNOWN, metered Grok exhaustion preserved, and WSL handoff evidence that does not silently promote native polls. The candidate also repairs the prior FAIL findings: WSL non-handoff tests pin `HERD_QUOTA_HANDOFF_REQUIRED=0` and clear `HERD_QUOTA_HANDOFF_BIN`; production `liveRouteCount` counts authenticated-account occupancy rather than exact model/pool; default handoff freshness is 300s cadence plus 30s delivery margin (330s), not 2 minutes.

## Production read

- Grok probe argv is `grok --model <model> --always-approve --no-subagents --disable-web-search -p <sentinel prompt>` with empty stdin. Advertising probe attaches stdin only when nonempty, so `-p` is not forced onto a pipe.
- Native Grok billing distinguishes HTTP/auth/decode/error-under-200/missing-contract (UNKNOWN) from `total==0,used==0,remaining==0` (unmetered, no window) and positive totals (metered credits, no invented reset). Plan text alone is not availability.
- WSL auto-requires handoff via microsoft osrelease; invalid/stale/ambiguous/missing handoff becomes `QuotaHandoffError` and ComputeAll marks every provider unavailable with reason `quota-handoff-error` instead of promoting native polls. Strict review Decide also rejects `QuotaHandoffError`.
- `liveRouteCount` counts working/starting agents of the same provider kind. `herd route` installs it; Pick records exact spark pool identity when shared Codex slots fill.

## Verification (this WSL2 host)

- `go test -count=1 ./pkg/usage ./pkg/router` PASS
- `go test -count=1 ./cmd/herd` PASS (105.923s), including FAC-670 usage/route/live-count tests
- Mutation (pool only, restored, tree clean afterward):
  - drop Grok `-p` → `TestFAC670GrokProbeRunsHeadlessWithExactSentinel` RED (exit 41, missing `--no-subagents`/`-p`)
  - `defaultQuotaHandoffMaxAge=2m` → `TestFAC670HandoffFreshnessCoversApprovedCadence` RED (5m29s rejected, max 2m)
  - occupancy limited to spark-named agents → `TestFAC670LiveRouteCountUsesProviderAccountOccupancy` RED (got 1, want 2)
  - restoring the candidate made those three GREEN again

## Residual risk (non-blocking)

- `LiveCount` is wired only on `herd route`. Lane launch and pool-review Decide still omit it; that is pre-existing, not a regression of this SHA. Occupancy truth is in route JSON and the shared-account test, not in every admission path.
- `BurnState` `used`/`remaining` `omitempty` hides genuine zero measurements in JSON; unmetered is still explicit via `entitlement`/`reason`, and the usage CLI no longer prints 0/0 for flat-rate.
- Graph `detect-changes` vs merge-base `45330b43773b8b86e80a439cb90a3479232b343e` scored 0.85 with helper test-gap noise (`pinNonHandoffQuotaTest`, `runUsage`); the FAC-670 tests cover those shipped paths.

No blocking defect remains on this exact revision.
