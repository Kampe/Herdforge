sha: 0f177d48891f6492bfbbb4f52c1c23fd70f2b71c
branch: recovery/fac-778-standing-publication
task: FAC-778
reviewer: review-fac-778-0f177d48891f
reviewer-family: anthropic
builder-family: openai
verdict: PASS
reviewed-base: 4180d2b79149bf6fa9041d65be9732e7e8605bfd
reviewed-head: 0f177d48891f6492bfbbb4f52c1c23fd70f2b71c
---

This supersedes only the "Tests run" root-cause narrative in
`0f177d48891f-review-fac-778-0f177d48891f-toolchain.md` (that artifact and the
original BLOCKED artifact are both left unchanged; this is a new file, same
reviewer identity, same base/head, refined evidence). The two commits are
still reviewed together as one unit; scope and static review are unchanged
from the prior artifact and not repeated in full here.

## Verified host toolchain (command-scoped only, no global mutation)

```
$ PATH="$HOME/.local/state/herdforge-tools/git-2.55.0-complete/bin:$HOME/.local/share/mise/installs/go/1.26.4/bin:$PATH"
$ git --version
git version 2.55.0
$ command -v git
/home/kampe/.local/state/herdforge-tools/git-2.55.0-complete/bin/git
$ git merge-tree --help    # exit 0, has --write-tree
merge-tree available
$ go version   # (unchanged from prior artifact)
go version go1.26.4 linux/amd64
```

PATH was set inline per-command only (each tool invocation is its own shell);
nothing was exported to shell config, and no software was installed.

## Root cause of the previously-reported cmd/herd failure

The system-default `git` on this host is 2.34.1:

```
$ /usr/bin/git --version   # (default PATH resolution, unscoped)
git version 2.34.1
```

`git merge-tree --write-tree` (real three-way merge without touching the
index) is not available in 2.34.x; it requires a materially newer Git.
`TestObserveVerifyLandedSquashPreservesCandidate`'s patch-equivalence proof
depends on that capability. Re-run with only the verified 2.55.0 prefix
ahead on PATH, at the unchanged candidate HEAD:

```
$ git rev-parse HEAD
0f177d48891f6492bfbbb4f52c1c23fd70f2b71c
$ git status --porcelain
(clean)
$ go test ./cmd/herd/... -run TestObserveVerifyLandedSquashPreservesCandidate -v -count=1
=== RUN   TestObserveVerifyLandedSquashPreservesCandidate
--- PASS: TestObserveVerifyLandedSquashPreservesCandidate (0.09s)
PASS
ok  	github.com/Kampe/Herdforge/cmd/herd	0.096s
```

This confirms the failure recorded in the prior artifact's "Tests run"
section was caused by the legacy system Git's missing `merge-tree
--write-tree` capability, not by this candidate's diff — it is not a
regression in `standing.go`/`goalguard.go`/`main.go`, and not evidence this
candidate's changes are unsound. I do not claim this proves the fixture is
unconditionally green everywhere: the coordinator has independently observed
separate WSL whole-CI health-fixture failures via a parallel Mac `make ci`
run. Those are a distinct, pre-existing host/environment matter outside this
candidate's diff and outside what a targeted re-run here can rule in or out;
I am not asserting a full-suite PASS on WSL, only that this one specific,
previously-flagged fixture is toolchain-caused and passes under the correct
verified toolchain.

## Relevant standing/goalguard/publication tests, same verified toolchain

```
$ go test ./pkg/standing/... ./pkg/goalguard/... -v -count=1
... (46 tests, all PASS, incl. TestLanePushStandingGrantMatchesRenderedAndDurableAuthority
     and TestLanePushStandingGrantFailsClosedForNonWritersAndProtectedBranches)
ok  	github.com/Kampe/Herdforge/pkg/standing	0.035s
ok  	github.com/Kampe/Herdforge/pkg/goalguard	0.564s
$ git rev-parse HEAD
0f177d48891f6492bfbbb4f52c1c23fd70f2b71c
$ git status --porcelain
(clean)
```

## Compiling negative control

Not repeated here per instruction to preserve prior logs and avoid re-running
whole suites gratuitously. See
`0f177d48891f-review-fac-778-0f177d48891f-toolchain.md` for the full
transcript: swapping `pkg/standing/standing.go` for its pre-fix (`5fa955d1`)
blob reproduces the exact described defect (`master` granted `AllowedBranch`
under an unknown default branch) and fails
`TestLanePushStandingGrantFailsClosedForNonWritersAndProtectedBranches`,
confirming the new tests are non-vacuous; the fixed file was restored and the
tree confirmed clean at the exact candidate SHA before that artifact was
written. That evidence is unaffected by git toolchain version (pure Go unit
test, no git subprocess involved) and is not re-run here.

## Residual risk

None identified in this candidate's diff. The only test-suite anomaly
encountered (`TestObserveVerifyLandedSquashPreservesCandidate`) is
attributable to the legacy system Git lacking a capability the fixture
requires, confirmed resolved under the verified 2.55.0 prefix, and is not a
finding against this candidate. Any other WSL-specific health-fixture
failures the coordinator is independently tracking are noted as a separate,
unrelated environment matter, not ruled in or out by this review.

## Verdict

PASS.
