sha: 2eac942569c39f356404ad175d9991638d22217d
branch: recovery/mender-provenance-claude-1600
task: FAC-797
reviewer: review-fac-797-2eac942569c3
reviewer-family: open-weight
builder-family: anthropic
verdict: PASS
reviewed-base: f214a3e9ebf10f178af9f0310a537c3637ce3e63
reviewed-head: 2eac942569c39f356404ad175d9991638d22217d
---

## Findings and risk

patch_id: dba2b71b236f646d2dc1822f0d9b41fe4837aa04
verification_digest: sha256:a728518ac6f8a2d9a1ca48be7e30968792ce519ba01cf3a6cf7ab192bff69f69
full_range: f214a3e9ebf10f178af9f0310a537c3637ce3e63..2eac942569c39f356404ad175d9991638d22217d

The verification_digest above is the coordinator-supplied binding for retained evidence (.worktrees/orchestrator/.herd/coordinator-resume/completion-recovery-20260909/fac797-review-evidence-1620.json); I did not access that file (outside my leased surface) and bind it as supplied, not as my own receipt.

Reviewed range: exactly f214a3e9ebf10f178af9f0310a537c3637ce3e63..2eac942569c39f356404ad175d9991638d22217d — one commit, 3 files, all under pkg/provenance/ (provenance.go, fac601_external_test.go, new vcs_contradiction_test.go), confirmed via git show --stat. Risk floor: R2 (herd review-classify effective=R2, evidence r2.production_source; matches the card's bounded repair risk R2). Reviewer family open-weight (OpenCode/litellm/lazer/glm-5.3-flash) differs from author family anthropic as required for R1–R3.

Acceptance criteria, independently verified:
1. Both-present conflicting revisions hard-fail externally and in-process — MET. binaryValuesFrom (pkg/provenance/provenance.go:281-336) now reads the stamp and vcs.revision in one pass; on both-present-differ it returns empty revision plus contradiction (provenance.go:324-326). Validate checks VCSContradiction FIRST (provenance.go:163-165) unconditionally of Comparable. In-process seam (path=="", BinaryRevision global vs debug.ReadBuildInfo) flows through the identical comparison line and into CurrentExecutable's Info.
2. No bypass through Comparable=false / empty revision — MET. Validate ignores Comparable entirely; the contradiction is carried into Info by infoFor (provenance.go:141-155), executableInfo (provenance.go:214-229), and CurrentExecutable (provenance.go:61-62). BinaryRevision is blanked on contradiction, so a caller that only later calls Validate gets the explicit "no source revision metadata" hard error rather than availability. All traced public consumers verified fail-closed: cmd/herd/main.go:1218/1231 (Read+Validate), 9243/9249/9253 (Read+Validate+ValidateInstalled), 106 (CurrentExecutable, display-only --version); pkg/harvest/integration_runtime.go:83/90 (inspect: !Comparable errors outright, Validate rejects contradiction) and 163/229 (blanked BinaryRevision fails fullIntegrationSHA -> "identity is unknown", fail-closed); pkg/security/hostcreds_mitm.go:789 (Resolve). ValidateInstalled reaches the shared gate via validateInstalledInfos (provenance.go:132-139).
3. Matching metadata succeeds; missing-metadata/dirty semantics explicit — MET. Agreeing fixture passes; malformed/duplicate stamp still returns UNKNOWN before any comparison (provenance.go:309-314); vcs.modified is never read; dirty-vs-mismatch remain distinct.
4. Real tiny Go fixture with conflicting actual VCS vs stamp fails even when the stamp matches the requested source — MET. vcs_contradiction_test.go builds real `go build -buildvcs=true` binaries from a throwaway git-tracked github.com/Kampe/Herdforge-named module (matching selectedLinkerStamp's hardcoded prefix), stamps the FIRST commit while actually compiling the SECOND; asserts contradiction names both SHAs, empty revision, ReadExecutable carry-through, Validate rejection against BOTH SHAs, and Resolve refusal via HERD_BIN. No synthetic debug.BuildInfo anywhere.
5. Compiling guard removal makes the unchanged assertion RED, restore GREEN — MET (I reproduced it myself; see Tests run).

Numbered findings:
1. (Documentation/intent mismatch, non-blocking) The committed comment in binaryValuesFrom (provenance.go:271-274), the commit message, and the retained author report all claim a worktree build carrying "the common checkout's vcs.revision" alongside a differing worktree-HEAD ldflags stamp is "expected and the stamp is trusted, by design". ACTUAL CODE DOES THE OPPOSITE: both-present-differ is unconditionally rejected. I verified the underlying premise empirically: in linked git worktrees (.git is a regular FILE — as in .worktrees/* and .herd/pool-*/*), Go's buildvcs stamping only recognizes .git DIRECTORIES (cmd/go/internal/vcs/vcs.go vcsGit.RootNames isDir:true), walks UP to the canonical checkout, and stamps ITS head — a throwaway binary I built in this pool worktree embedded vcs.revision=4074852990241458e8208b50e6821b1bff5362e7 (the canonical checkout's HEAD, an ancestor of this candidate) with vcs.modified=true. A real linked-worktree fixture probe confirmed worktree HEAD == stamped HEAD produced NO contradiction only because the binary embedded NO native vcs.revision at all. Operational consequence: `make build` (scripts/build-herd.sh:28,37) inside any linked worktree stamps BinaryRevision=worktree HEAD while Go embeds canonical HEAD, so the resulting bin/herd is now hard-rejected by Validate/ValidateInstalled against its own worktree (previously accepted via stamp-only trust). I judge this non-blocking: the card's acceptance criteria literally demand exactly this strict hard failure, the old stamp-only worktree "trust" had no independent verification value (the native stamp was a Go walk-up artifact, not evidence of build source), and canonical-checkout/CI builds (.git as a directory) stamp their own HEAD and are unaffected. But the author's "worktree behavior unchanged" claim is FALSE and the in-code comment misdescribes the behavior it documents; recommend a follow-up correcting the comment and reporting the worktree-build consequence via a new immutable artifact.
2. (Test-coverage limitation, non-blocking) No dedicated in-process contradiction test exists, and I proved one cannot currently fire in unit tests: `go test` binaries carry NO vcs settings at all (the synthesized main package is built in the cache outside any repo — verified by dumping debug.ReadBuildInfo settings: no vcs/vcs.revision/-buildvcs keys), so nativeRevision is always "" and the contradiction branch (which requires nativeRevision != "") cannot trigger in-process under go test. The in-process seam is therefore covered structurally only: it shares the exact comparison line proven load-bearing by my external-fixture RED proof, and the in-process stamp-source selection is covered by fac601_external_test.go. The author's report claims in-process coverage through "the one shared gate" without disclosing this environmental limit; noted as a reporting accuracy issue, not a code defect.
3. (Disclosed, acceptable) ValidateInstalled has no dedicated contradictory-fixture test; it is covered transitively through validateInstalledInfos -> Validate, exactly as the author's limitations section states.

Residual risk: worktree-local stamped builds now fail their own worktree self-gates (finding 1) — a deliberate-strictness consequence of the card, but the fleet should be aware lanes building via make build inside .worktrees/* or pool slots will see "stale herd binary: -ldflags binary revision ... contradicts native Go vcs.revision ..." until build provenance for linked worktrees is redesigned. I ran the focused pkg/provenance suite, not the full repo suite or Linux gate (root-owned per packet); the coordinator digest binds retained full-range evidence I did not independently re-verify.

## Tests run

All inside the leased pool surface (git rev-parse --show-toplevel resolved to .herd/pool-fac708-gemini-flash-0342/pool-01 before every mutating step); zsh direct exit capture before any tail, no PIPESTATUS:

- go build ./pkg/provenance/... -> exit 0. go vet ./pkg/provenance/... -> exit 0.
- go test -race -count=1 ./pkg/provenance/... (GOMAXPROCS=2 GOFLAGS=-p=2) -> ok github.com/Kampe/Herdforge/pkg/provenance 4.263s, exit 0, all 11 tests including all pre-existing tests.
- Focused new tests verbose: TestBinaryValuesFromRejectsContradictoryLDFlagsAndNativeVCS PASS (0.74s), TestBinaryValuesFromAcceptsAgreeingLDFlagsAndNativeVCS PASS (0.39s).
- RED mutation proof: pre-mutation sha256 of provenance.go = 734940210a44965248540fdedfdd3d5aa3eed11ae279f86c50e5d76a0f25b629 (matches the author's reported restored digest); edited the exact added guard to `if false && stampFound && nativeRevision != "" && nativeRevision != stampRevision {`; go build ./pkg/provenance/... -> exit 0 (removal still compiles); go test -race -count=1 ./pkg/provenance/... -run 'TestBinaryValuesFromRejectsContradictoryLDFlagsAndNativeVCS' -> FAIL with raw assertion `expected a VCS contradiction, got none (revision="912a5ac64ead8bbc585cd46a88a86ccb98b2d0d0" ...)`, direct captured exit code 1; restored via git checkout -- pkg/provenance/provenance.go, post-restore sha256 identical 7349...b629, git status --porcelain empty.
- GREEN after restore: go test -race -count=1 ./pkg/provenance/... -> ok 4.275s, direct exit code 0.
- Reviewer scratch probes (untracked files, deleted afterward, tree confirmed clean): (a) in-process seam probe — set BinaryRevision global, called binaryValuesFrom("")/CurrentExecutable(): test binary carries no vcs settings, contradiction cannot fire in-process under go test (finding 2 evidence); (b) linked-worktree fixture probes and a go build -o /tmp probe binary + go version -m: worktree builds embed the canonical checkout's HEAD (vcs.revision=40748529..., vcs.time matching that commit exactly, vcs.modified=true), worktree-HEAD stamp agreements were vacuous (no native stamp embedded) (finding 1 evidence).
- herd review-classify recovery/mender-provenance-claude-1600 --pin 2eac942569c39f356404ad175d9991638d22217d -> tier=R2 (inferred=R2, explicit-floor=none), exit 0.
- Read-only caller trace: git grep for provenance.{Validate,ReadExecutable,Resolve,ValidateInstalled,Read,CurrentExecutable} across all packages; every production consumer enumerated in finding/acceptance analysis above.
