sha: a41d0d0c49aad96958307e9be9f86e1643692b23
branch: harvest/fac791-native-auth-0810
task: FAC-791
reviewer: review-fac-791-a41d0d0c49aa
reviewer-family: openai
builder-family: open-weight
verdict: BLOCKED
reviewed-base: 9247a9f492bb4874962f05e71a61ac39f6fa25eb
reviewed-head: a41d0d0c49aad96958307e9be9f86e1643692b23
---
## Findings and risk

Exact reviewed range: `9247a9f492bb4874962f05e71a61ac39f6fa25eb..a41d0d0c49aad96958307e9be9f86e1643692b23`.
The classifier independently sets the required risk floor to R3 (`r3.auth_secrets` and
`r3.core_orchestration`). The acceptance criteria are native OpenCode auth classification,
preflight ordering and exact packet use, no secret export or provider/cap bypass, typed
refusals for unknown/logged-out/probe-error states, and focused plus high-risk verification.

1. No source finding remains from this bounded review. `harnessAuthKinds` now includes
   `AuthorKindOpenCode`, and `DiagnoseKindAuthReadiness` enters the native path before any
   HostCreds resolution. `IsSupportedAuthorKind` remains HostCreds-only, while
   `StartHostCredsSession` and `StartAuthorLive` continue to reject OpenCode, preserving the
   raw broker boundary. Unknown kinds remain `unknown_kind`; explicit logged-out Claude and
   unknown login states remain distinct and fail closed where proven.

2. BLOCKED operational acceptance gap: the packet requires an actual cold OpenCode review
   launch that consumes the exact review packet. No coordinator-owned cold-canary evidence
   for this exact candidate head was provided to this reviewer. The retained WSL report is
   for candidate `6cf5dfad9deb6524414f78b2c53a1745edfa0f57`, not this head, and is explicitly
   coordinator-owned CI rather than independent exact-head or native-closure evidence. I
   therefore cannot establish exact packet consumption or grant merge authority. Supply the
   exact-head canary evidence and re-run/admit this review; do not treat the missing canary
   as a PASS.

Residual risk: OpenCode has no machine-readable login probe in this change, so its login
state remains honestly `unknown` and the secondary request probe/launch-time checks remain
the backstop. The classification itself does not copy keys, manufacture environment
credentials, refresh auth, broaden HostCreds allowlists, or alter FAC-170 authority.

patch_id: 6d2c7401f8ae02a84de4c2f439acccde860c1bff
verification_digest: sha256:b845c853db49046ee32e2dae3551bab7b42dfaa307b3045a0cf093e3fa11995b
full_range: 9247a9f492bb4874962f05e71a61ac39f6fa25eb..a41d0d0c49aad96958307e9be9f86e1643692b23

## Tests run

- `code-review-graph status --repo "$repo_root" --json` — exit 0; 343 nodes, 5591 edges,
  7 indexed files, built at exact head `a41d0d0c49aad96958307e9be9f86e1643692b23`.
- `code-review-graph detect-changes --repo "$repo_root" --base 9247a9f492bb4874962f05e71a61ac39f6fa25eb --brief` — exit 0; 12 changed files, 17 changed functions, overall graph risk 0.50, five test-gap signals.
- `herd review-classify harvest/fac791-native-auth-0810 --pin a41d0d0c49aad96958307e9be9f86e1643692b23 --json` — exit 0; effective tier R3.
- `make preflight` — exit 2; workspace boundary, signal-literal, and merge-policy checks
  passed, then the branch-drift gate reported `main` 0 commits ahead and `origin/main` 62
  commits ahead.
- `go test ./pkg/security ./cmd/herd` — exit 1. `pkg/security` passed in 83.738s; the
  existing broad `cmd/herd` run had three FAC601 integration failures caused by the host
  disk-capacity gate (`free_percent` about 0.63 below the 2% reserve policy), not by the
  reviewed auth paths.
- `go test ./pkg/security -count=1 -run 'Test(ClaudeNeedsNoBrokeredHostCredential|HarnessAuthenticatedKindsClassifyNative|OpenCodeDiagnosisIsBrokerable|ClaudeDiagnosisIsBrokerable|ClaudeDeterministicAuthStatusProbes|UnknownLoginIsNotLoggedOut|DiagnoseKindAuthReadiness_(NoAPIKeys_External|RawAPIKeyNotProductionAuthority|AGYNative))'` — exit 0.
- `go test ./cmd/herd -count=1 -run 'Test(OpenCodeReviewerPassesAuthPreflight|Preflight(SupportedKindsBrokerable|RefusesLoggedOutClaudeWithoutLeaseOrTab|RefusesLoggedOutClaudeFakeBinaryOnPATH|OpenCodeProbeError)|HostCredsCLI_(HarnessKindIsNativeAuthWithoutEnvKeys|UnknownKindRejected|UsageIncludesOpenCodeDiagnose))'` — exit 0.
- Targeted race checks for the same security and preflight/auth CLI tests — exit 0 for both
  `go test -race ./pkg/security ...` and `go test -race ./cmd/herd ...`.
- `go vet ./pkg/security ./cmd/herd` — exit 0.
- `go build ./cmd/herd` — exit 0.
- RED/GREEN non-vacuity proof: after removing only `AuthorKindOpenCode: true` from
  `pkg/security/harness_auth.go`, `go test ./pkg/security -count=1 -run '^TestOpenCodeDiagnosisIsBrokerable$'`
  failed exit 1 with the expected `unknown_kind` diagnosis. Restoring the exact candidate
  blob made the same test pass exit 0; final `git status --short --untracked-files=all` was
  clean and `git diff --check` passed.
