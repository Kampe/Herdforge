sha: 58e32fe2148dcff15ef6d23745262a884828354a
branch: herd/fac-765
task: FAC-765
reviewer: review-fac-765-58e32fe2148d
reviewer-family: anthropic
builder-family: xai
verdict: PASS
reviewed-base: 58e32fe2148dcff15ef6d23745262a884828354a^
reviewed-head: 58e32fe2148dcff15ef6d23745262a884828354a
---
## Findings and risk

Reviewed range: single commit 58e32fe2148d ("test: prove harvest latestExact
against shared-reachability FAC-765") against its parent, in the leased pool
worktree at .herd/pool-fac765/pool-02 (detached HEAD at the candidate;
`refs/remotes/origin/herd/fac-765` points at this exact SHA, hence
`branch: herd/fac-765` above).

Risk tier: R0. This commit touches only
`cmd/herd/harvest_branch_scope_test.go` (+18/-2, one test function) — no
production code path changes. I still applied R2-level scrutiny to
non-vacuity because the test guards a trust-critical harvest-selection path
(`resolveHarvestCandidateWithReconstructionAt` in `cmd/herd/reviewingest.go`).

### Context: this is a direct follow-up to a prior BLOCKING finding

The immediate parent chain includes f88780fa490c, which added the
`latestExact` branch-scoped tie-break to `resolveHarvestCandidateWithReconstructionAt`
(lines 1469-1504: alongside the existing timestamp-only `latest` computed over
all rows reachable from the target branch, it separately tracks the latest
row whose `Branch` string exactly equals the requested branch and prefers it
when non-empty). That commit was reviewed and received a FAIL
(`.herd/review/inbox/f88780fa490c-review-fac-765-f88780fa490c.md`): its only
test, `TestHostLabelledHarvestSelectsExactBranchBoundPass`, built two sibling
branches with no merge between them, so `reachableFromBranch`'s ancestry
filter alone excluded the off-branch SHA before either `latest` or
`latestExact` was ever compared — the test passed even with the entire
`latestExact` branch deleted.

### This commit's fix, and why it is now non-vacuous

This diff changes exactly that fixture, in line with correction (a) from the
FAIL verdict:

1. After creating `herd/fac-652` and `herd/fac-670` off the same base, it
   checks out `herd/fac-652` and does `git merge --no-ff herd/fac-670`, then
   asserts both `reachableFromBranch(root, fac652, "herd/fac-652")` and
   `reachableFromBranch(root, fac670, "herd/fac-652")` are true. This
   explicitly proves the ancestry filter can no longer be the sole explanation
   for the branch selection — both SHAs now clear it.
2. It records the *exact*-branch PASS (fac652, `Branch: "herd/fac-652"`)
   first with `l.Now` stamped to 2026-09-07T01:00:00Z, then the *off-branch*
   PASS (fac670, `Branch: "herd/fac-670"`) with `l.Now` stamped to
   2026-09-07T02:00:00Z — the later timestamp.

I traced the iteration in `resolveHarvestCandidateWithReconstructionAt`
directly against this fixture: both rows pass `reachableFromBranch(...,
"herd/fac-652")` now (neither is filtered into `offBranch`). Iterating,
`latest` tracks the max-timestamp row regardless of order (`row.Timestamp >=
latest.Timestamp`), so `latest` ends up as fac670 (02:00 > 01:00). `latestExact`
only updates when `row.Branch == "herd/fac-652"`, which only the fac652 row
satisfies, so `latestExact` ends up as fac652. Since `latestExact.SHA != ""`,
`report.LastPassSHA = fac652`. If the `latestExact` branch were deleted (i.e.
reverting exactly the change f88780fa introduced), selection would fall back
to `latest.SHA = fac670`, which trips this test's own assertion
(`if got.LastPassSHA == fac670 { t.Fatalf(...) }`). That is a genuine
RED-on-revert / GREEN-on-fix relationship, established by static trace of the
exact code path (see Tests run for why I did not execute this mechanically).

No blocking or non-blocking findings. The two added reachability assertions
(step 1 above) are good defensive coverage against future topology drift
silently re-introducing vacuity, and the timestamp ordering is the correct
minimal construction to separate `latest` from `latestExact` behavior.

## Tests run

No commands executed. This host has no `go` binary on PATH (`command -v go`
reports not found), matching the same environment limitation recorded in the
prior f88780fa490c review of this same test path — I could not run `go test
./cmd/herd/... -run TestHostLabelledHarvestSelectsExactBranchBoundPass` or any
mutation (revert-latestExact-and-confirm-RED) check. The non-vacuity
conclusion above is derived from static reading of `reachableFromBranch`
(`git merge-base --is-ancestor` semantics), the exact `latest`/`latestExact`
loop in `cmd/herd/reviewingest.go:1469-1504`, and the fixture's git topology
and timestamps as written in the diff — not from an observed test run. I did
not swap or revert any tracked file in this worktree to attempt to
demonstrate this mechanically, since no toolchain is present to execute the
result either way and the working tree must stay clean per the reviewer
contract's safety rules; `git status --porcelain` is clean at end of review.

## Residual risk

None beyond what the prior review already flagged as covered by
`host_ingest.go`'s existing test suite. This commit closes the specific
BLOCKING gap raised against f88780fa490c without touching any production
code, so it carries no new production risk; the only residual dependency is
that nobody has re-run `go test` on this exact fixture on a host with a Go
toolchain to convert this trace into an observed RED/GREEN result.
