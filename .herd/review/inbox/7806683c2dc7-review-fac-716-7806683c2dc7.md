sha: 7806683c2dc76d691e89c93b1bf30cafc2bcdcd6
branch: recovery/fac-716-flash-slot-reentry
task: FAC-716
reviewer: review-fac-716-7806683c2dc7
reviewer-family: openai
builder-family: open-weight
verdict: PASS
reviewed-base: 2fdfbe56cff5ce114c98b7258aa79e4425b7b6ae
reviewed-head: 7806683c2dc76d691e89c93b1bf30cafc2bcdcd6
---

## Review

PASS. The exact two-commit range was reviewed, not only the tip. It contains only the permitted test-layer changes in `cmd/herd/main_test.go` and `cmd/herd/slotreentry_test.go`; production source is unchanged.

The fix preserves the verifier-owned `slot.EnvHeld=1` marker across `laneenv.Strip()` only when it was present at process entry (`cmd/herd/main_test.go:91-124`). It still strips all other lane metadata, and fails closed if restoring the marker fails. The companion test checks both preservation and absence of every other leaked launch variable (`cmd/herd/slotreentry_test.go:23-33`). The nested CLI test is bounded at 15 seconds and only runs when a real outer verifier marker is present (`cmd/herd/slotreentry_test.go:45-64`), so plain `go test` does not fabricate authority.

The packet-supplied accepted native receipt is consistent with the source and reports the required real outer-slot RED-to-GREEN behavior. I did not recreate managed verification with a manufactured marker, change slot counts, or relax timeouts. No correctness, ownership-boundary, fail-closed, concurrency, portability, or test-vacuity finding remains for this revision.

## Graph and scope

`code-review-graph 2.3.7` was fully rebuilt at the candidate SHA: 16,777 nodes, 210,845 edges, 1,421 files. Exact-range analysis reported 2 changed files, 3 changed test functions/helpers, 0 affected flows, and 0 test gaps. The graph-backed file summaries and direct diff agree that the review surface is limited to the two packet-authorized files.

## Tests run

- `go test ./cmd/herd -run 'Test(SlotReentryAuthorityRetainedForNestedCLIChildren|NestedCLIChildRetainsHeldSlotAuthority)$' -count=1 -v` — exit 0; companion passed and managed-only child test skipped because this Mac lane had no verifier-owned outer slot.
- `go test ./cmd/herd -run 'TestFreshBuildCLI_GoCleanDoesNotLieAboutStaleDist' -count=1 -v` — exit 0.
- `go test -race ./cmd/herd -run 'Test(SlotReentryAuthorityRetainedForNestedCLIChildren|NestedCLIChildRetainsHeldSlotAuthority)$' -count=1 -v` — exit 0; same managed-only skip.
- `go build ./cmd/herd` — exit 0.
- `go vet ./cmd/herd` — exit 0.
- `go run ./scripts/hermeticity/` — exit 0.
- `gofmt -d cmd/herd/main_test.go cmd/herd/slotreentry_test.go; git diff --check 2fdfbe56cff5ce114c98b7258aa79e4425b7b6ae..7806683c2dc76d691e89c93b1bf30cafc2bcdcd6 -- cmd/herd/main_test.go cmd/herd/slotreentry_test.go` — exit 0.
- `go test ./cmd/herd -count=1` — terminated with exit 143 after more than two minutes without test output; no failure was observed. The coordinator-owned broad WSL gate remains authoritative, and the packet’s known missing-`mise` hermetic toolchain fixture is pre-existing rather than a candidate finding.
- Reversible pre-fix checkout/mutation attempt using `git checkout 4f8f0f6d7b532f8d8402da1543364e6ac7be9ff7 -- cmd/herd/main_test.go`, followed by `go run ./cmd/herd slot with -- go test ...`, then restoration from `7806683c...` — exit 0, but not admitted as a RED control because `pkg/slot.With` does not stamp `EnvHeld`; the candidate was restored and the worktree is clean. No synthetic `EnvHeld` evidence was used.

## Findings

### Blockers

None.

### High-Priority

None.

### Medium / Suggestions

None.

### Nitpicks

None.

## Verdict details

task_ref=FAC-716, candidate_sha=7806683c2dc76d691e89c93b1bf30cafc2bcdcd6, patch_id=4f8f0f6d7b532f8d8402da1543364e6ac7be9ff7..7806683c2dc76d691e89c93b1bf30cafc2bcdcd6, risk_tier=R3, verdict=PASS

author_family=open-weight, reviewer_family=openai, verification_digest=graph:16777-nodes/210845-edges;focused:pass;race:pass;build:pass;vet:pass;hermeticity:pass

findings=none; residual_risk=managed WSL verifier receipt and coordinator-owned broad gate were not rerun in this Mac recovery lane; reviewed_at=2026-09-09T16:46:51Z
