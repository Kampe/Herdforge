sha: 349c0b6e1e082d03785889a69fa4d1f42d8c9d2f
branch: repair/fac-765-authenticated-host-ingest
task: FAC-765
reviewer: review-fac-765-349c0b6e1e08
reviewer-family: openai
builder-family: xai
verdict: FAIL
reviewed-base: 9737258c9ecd7a48da215ea729cceff3cbc1d011
reviewed-head: 349c0b6e1e082d03785889a69fa4d1f42d8c9d2f
---
## Findings and risk

Reviewed all eleven commits as one production range, exactly
9737258c9ecd7a48da215ea729cceff3cbc1d011..349c0b6e1e082d03785889a69fa4d1f42d8c9d2f,
on repair/fac-765-authenticated-host-ingest. This is R3 and the risk floor is
retained. Builder provenance is the packet-admitted xai family; reviewer family
is independent openai. The graph was current at the reviewed head (325 nodes,
3,215 edges, 15 indexed Go files), but it reported no stored flows and only
partial repository coverage, so command-layer callers were audited directly.

The native reviewer LaunchDecision -> RecordStarted -> accepted receipt ->
host-ingest path, exact locator checks, member/StartToken/session/family
refusals, same-host retry projections through the required reviewledger
consumers, cross-host dissent in both append orders, candidate display tests,
and the positive/negative producer tests pass. The candidate nevertheless
fails the complete acceptance criteria:

1. [Blocker] A generic candidate-level EventSupersession can clear a current
   reviewer veto. `pkg/reviewledger/host_ingest.go:115-123` adds every
   EventSupersession whose `SHA` equals the candidate and whose `Reviewer` is
   non-empty to the host retry-supersession index, without requiring the event
   to be the PASS RetryOf event for that host. `Eligible` consumes this index
   in `pkg/reviewledger/operations.go:766-767` and can therefore skip a real
   FAIL/BLOCKED row. An independent temporary fixture with a current candidate
   reviewer-a FAIL, reviewer-b PASS, and a generic `Supersession{SHA: current,
   PreviousSHA: old, Reviewer: reviewer-a}` returned `eligible=true, err=nil`.
   This is a fail-open admission path: an identity-replacement/supersession
   event unrelated to a retry can authorize the candidate by erasing dissent.

2. [High] Same-host RetryOf PASS is still treated as whole-SHA supersession by
   two real command-layer consumers. `cmd/herd/attention_candidates.go:125-147`
   maps `EventSupersession.Task` directly to `superseded[SHA]`, so the retry
   event emitted for the same candidate causes `collectAttentionCandidates` to
   drop a review-ready candidate before live integration evidence is shown.
   `cmd/herd/legacy_review_authority.go:57-72` similarly marks every
   EventSupersession SHA revoked, so legacy closure cannot find the retry PASS.
   Temporary real-consumer fixtures both failed: an authenticated same-host
   reviewer-b PASS RetryOf reviewer-a returned no attention candidate, and the
   legacy closure returned `no current admitted PASS`. This violates the
   required deterministic host-aware projection and the requirement that
   EventSupersession preserve the candidate SHA and pertinent retry evidence.

Residual risk: merging this exact head leaves a fail-open veto bypass and can
hide or strand valid same-host retry work in integration/closure flows. The
required focused ledger and candidate-display tests do not exercise these
command-layer and generic-event cases. The retained historical FAIL verdicts
and their bytes were not changed. No independent patch identity or verification
digest was supplied in the author handoff; no digest was fabricated here.

## Tests run

- `git rev-parse --show-toplevel`, branch, status, and `git rev-parse HEAD` —
  exit 0; isolated pool worktree, clean before artifact, HEAD exactly matched
  the packet candidate.
- `code-review-graph status` — exit 0; 325 nodes, 3,215 edges, 15 files,
  current at 349c0b6e. `detect-changes --base 9737258c... --brief` — exit 0;
  32 changed files, overall risk 0.60, partial index with 69 test gaps.
- `go test ./pkg/launch -count=1 -run
  'TestRecordStartedCopiesReviewerDecisionCandidateSHA|TestRecordStartedDoesNotInventBuilderCandidateSHA|TestAcceptedReviewLaunchForCandidate|TestAcceptedCanonicalMember|TestReachingBuilderReceipt'`
  — exit 0.
- `go test ./pkg/reviewledger -count=1 -run
  'TestSameHostRetryReachesEveryFinalConsumer|TestCrossHostRetryOrderPreservesVetoInBothAppendOrders|TestEligibleCrossHost|TestEligibleSameHost|TestRetryPASS'`
  — exit 0.
- `go test ./cmd/herd -count=1 -run
  'Test(ParseReviewHostIngest|ForgedReceipt|CanonicalReviewLaunch|CanonicalReviewProvenance|NativeStarted|HostIngestRefuses|LedgerReviews|LoadReapEvidenceCrossHostDissent)'`
  — exit 0.
- `go test ./pkg/review -count=1 -run TestVetoedKeepsCrossHostDissent` — exit
  0. `go test ./pkg/invariant -count=1 -run TestNoNewDuplicatedRules` — exit 0.
- Non-vacuity mutation: temporarily removed `CandidateSHA:
  decisionCandidateSHA(req)` from `pkg/launch/launch.go`; native producer test
  exited 1 with CandidateSHA empty. Restored the exact source; the same test
  then exited 0. Temporary fixtures were removed.
- Temporary real-consumer probes — both intentionally RED on the candidate:
  `TestFAC765SameHostRetryReachesAttentionPipeline` returned an empty result;
  `TestFAC765SameHostRetryReachesLegacyClosure` returned no current PASS;
  `TestFAC765GenericSupersessionDoesNotClearCurrentVeto` returned
  `eligible=true` without an error. All probe files were deleted afterward.
- `git diff --check 9737258c... 349c0b6e...` and final source/fixture checks —
  exit 0; source restored and pool worktree clean.
- `make preflight` — exit 0; boundary, signal-literal, merge-policy, and
  main/origin checks passed. It reported the known local no-fence-broker
  warning and stale go-run binary provenance only.
- Not run per packet: broad package suites, `make lint`, `make all`, `make ci`,
  coordinator integration/hosted CI, ledger ingestion, or pane delivery.
  The known package-wide hook-timeout and security-gate jq-null limitations
  remain separate evidence and were not reclassified.
