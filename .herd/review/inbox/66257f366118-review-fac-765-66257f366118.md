sha: 66257f366118826a3134becffbbe7d31e02f5b15
branch: repair/fac-765-authenticated-host-ingest
task: FAC-765
reviewer: review-fac-765-66257f366118
reviewer-family: openai
builder-family: xai
verdict: FAIL
reviewed-base: 9737258c9ecd7a48da215ea729cceff3cbc1d011
reviewed-head: 66257f366118826a3134becffbbe7d31e02f5b15
---
## Findings and risk

Reviewed the complete twelve-commit production range
`9737258c9ecd7a48da215ea729cceff3cbc1d011..66257f366118826a3134becffbbe7d31e02f5b15`
as one R3 review on `repair/fac-765-authenticated-host-ingest`. The packet's
independent builder provenance is `xai`; reviewer family `openai` is different.
The graph was rebuilt at the exact reviewed head and reports 17,712 nodes,
226,677 edges, and 1,528 indexed files. Direct inspection covered the native
review launch/receipt/host-ingest path, canonical member and locator checks,
all `EventSupersession`/`EventRevoked` consumers found in the affected review
and command packages, and the attention, legacy closure, admission, readiness,
pipeline, and candidate projections.

1. [High] `pkg/review/ledger.go:1127-1170` leaves `Ledger.VetoSHAs` unaware of
   the host-aware retry projection. It indexes the latest verdicts by
   `(SHA, reviewer, host)` but then marks every valid latest FAIL/BLOCKED as a
   SHA veto without applying the same-host PASS `RetryOf` suppression used by
   `Vetoed`, `LedgerSnapshot.Vetoed`, and `PassSHAs`. A temporary fixture with
   reviewer-a FAIL on host-a and reviewer-b PASS `RetryOf: reviewer-a` on the
   same host returned the candidate in `VetoSHAs` (RED). This violates the
   acceptance requirement that one deterministic retry projection reach every
   listed final consumer and can expose a stale veto to callers of this API.

The native launch path and exact canonical reviewer locator checks otherwise
retain the candidate/task/repository/lane/host/session/member bindings, and the
generic candidate identity supersession does not clear a current veto. The
valid same-host retry remains eligible, while cross-host FAIL/BLOCKED survives.
Because the VetoSHAs consumer is still incorrect, this exact R3 revision is not
mergeable. Historical FAIL evidence and the coordinator's separate integration
CI were not modified or reclassified.

## Tests run

- `code-review-graph build --repo .` followed by `status --json`: PASS at the
  exact head; 1,532 files parsed, 1,528 indexed files, 17,712 nodes, and
  226,677 edges. `detect-changes --base 9737258c... --brief`: PASS, 36 changed
  files, risk score 0.85. The index was complete after the forced rebuild.
- Focused `go test ./pkg/reviewledger -count=1 -run
  'Test(GenericSupersessionDoesNotClearCurrentVeto|SameHostRetryReachesEveryFinalConsumer|CrossHostRetryOrderPreservesVetoInBothAppendOrders|EligibleCrossHostRetryDoesNotHideHostAVeto|EligibleSameHostRetryClearsNamedVeto|EligibleCrossHostBlockedSurvivesRetry)$'`:
  PASS.
- Focused `go test ./cmd/herd -count=1 -run` covering
  `TestSameHostRetryReachesAttentionPipeline`,
  `TestSameHostRetryReachesLegacyClosure`, revocation/replacement withdrawal,
  candidate display, canonical launch, native started receipt, host/session
  mismatch, builder-locator refusal, pre-edit builder proof, and forged receipt
  refusal: PASS.
- Focused `go test ./pkg/launch -count=1 -run` covering native reviewer
  `CandidateSHA`, non-invented builder SHA, canonical membership/start-token/
  host-session checks, exact candidate/task locator binding, and reaching builder
  proof: PASS.
- Focused `go test ./pkg/review -count=1 -run
  'Test(VetoedKeepsCrossHostDissent|PassSHAs|VetoSHAs)$'`: PASS; this existing
  set does not cover same-host retry behavior in `VetoSHAs`.
- Temporary `pkg/review` regression fixture for same-host retry in `VetoSHAs`:
  RED on the candidate (`VetoSHAs` returned the vetoed SHA). The temporary file
  was removed.
- Guard-removal mutation removing the self-identity protection from
  `IdentityReplacementSHA`: `TestSameHostRetryReachesAttentionPipeline` and
  `TestSameHostRetryReachesLegacyClosure` both RED. Restoring the exact guard
  returned both tests GREEN. The worktree was restored and is clean.
- `make preflight`: PASS for workspace boundary, signal literals, merge policy,
  and main/origin drift. The known local no-fence-broker and stale go-run
  binary-provenance warnings remain warnings only.
- `git diff --check 9737258c...66257f36...`: PASS. No broad package suite,
  `make lint`, `make all`, `make ci`, or coordinator integration CI was run per
  the packet; known package-wide hook-timeout and security-gate jq-null limits
  remain separate evidence.
