sha: a8f371721124634a61ce0ff995a52d15abfcee68
branch: herd/fac-681
task: FAC-681
reviewer: review-fac-681-a8f371721124-r2
retry-of: review-fac-681-a8f371721124
reviewer-family: anthropic
builder-family: openai
verdict: PASS
reviewed-head: a8f371721124634a61ce0ff995a52d15abfcee68
---

## Scope

Independent re-review of the same candidate as the first artifact
(review-fac-681-a8f371721124): a8f371721124 ("fix: centralize merge-tree
flag FAC-681") on top of ec274c215378. Diff confirmed via `git show
a8f371721124634a61ce0ff995a52d15abfcee68`: a new `pkg/gitroot.MergeTreeWriteFlag
= "--write-tree"` constant plus a new `TestMergeTreeWriteFlag`, and three call
sites (`pkg/mergeadmit/reconcile.go`, `pkg/review/pipeline.go` x2) switched
from the inline `"--write-tree"` literal to the constant. No other behavior
change. This artifact does not overwrite or supersede the first artifact;
both are preserved.

## Process note

`docs/prompts/review-contract.md` (referenced by the dispatched packet) still
does not exist in this tree; the reviewer contract actually present is
`.herd/prompts/reviewer.md` plus `.herd/prompts/routing.md`, both read before
this review as instructed.

## Verification

- `git rev-parse --show-toplevel` -> `/home/kampe/Projects/Herdforge/.herd/pool/pool-01` (resolves under `.herd/pool/`, not the shared checkout; isolation held for the whole session).
- `git log -1 --format='%H %s'` -> `a8f371721124634a61ce0ff995a52d15abfcee68 fix: centralize merge-tree flag FAC-681`.
- `git show --stat a8f371721124634a61ce0ff995a52d15abfcee68` -> 4 files changed, 18 insertions(+), 3 deletions(-) (`pkg/gitroot/gitroot.go`, `pkg/gitroot/gitroot_test.go`, `pkg/mergeadmit/reconcile.go`, `pkg/review/pipeline.go`).
- `~/.local/share/mise/shims/go version` -> `go version go1.26.4 linux/amd64` (invoked by full path, PATH not modified).
- `~/.local/share/mise/shims/go build ./...` -> clean, no output, exit 0.
- `~/.local/share/mise/shims/go test ./pkg/gitroot/... ./pkg/mergeadmit/... ./pkg/review/...`:
  - `pkg/gitroot` -> `ok` (0.159s), includes `TestMergeTreeWriteFlag`.
  - `pkg/mergeadmit` -> FAIL: `TestProveEquivalentLandedContextChangedStack` (reconcile_test.go:259, "equivalent-patch proof failed ... candidate commit 1 (patch 781ece347cd3) has no patch-equivalent counterpart on the landed history").
  - `pkg/review` -> FAIL: `TestPipelineContract_ConflictEvidenceIsRebaseNeeded` (pipeline_contract_test.go:255, `Conflict:unknown`).
- `git --version` -> `git version 2.34.1` (system git, unmodified `/usr/bin/git`, no PATH changes; this is below the 2.38 minimum for `git merge-tree --merge-base` that `matchCombinedRangeReplay`/`mergeTreeCapable`/`freshness` depend on).
- Pre-existing-failure proof, run inside `.herd/pool/pool-01` only:
  - `PARENT=$(git rev-parse a8f371721124634a61ce0ff995a52d15abfcee68^)` -> `ec274c215378cb5c9b9050b7507a05baf642618d`.
  - `git checkout "$PARENT" -- pkg/gitroot/gitroot.go pkg/gitroot/gitroot_test.go pkg/mergeadmit/reconcile.go pkg/review/pipeline.go` -> the 4 changed files swapped to their parent-commit blobs, `git status --porcelain` showed exactly those 4 as `M`.
  - `~/.local/share/mise/shims/go test ./pkg/mergeadmit/... ./pkg/review/...` on the parent-blob tree -> the identical two failures, same test names, same assertion text (only the goroutine-local worktree temp path and a synthetic SHA embedded in the pipeline test's generated fixture differ, as expected from that test constructing fresh temp state per run).
  - `git checkout a8f371721124634a61ce0ff995a52d15abfcee68 -- .` -> candidate tree restored.
  - `git status --porcelain` -> empty (clean) after restore.
  - `git rev-parse HEAD` -> `a8f371721124634a61ce0ff995a52d15abfcee68` throughout (branch tip never moved).

## Verdict rationale

PASS, independently confirmed. The change is a mechanical, behavior-preserving
centralization of a single flag literal into a named constant used at all
three call sites; it builds clean and its own new unit test passes. The two
test failures present in this sandbox reproduce byte-for-byte in cause
(same root: git 2.34.1 lacks `merge-tree --merge-base`, added in git 2.38)
on the parent commit with this candidate's 4 files swapped back to their
parent blobs, so they are proven pre-existing and orthogonal to this diff,
not introduced or worsened by it. No source was edited; only a transient,
restored-before-verdict swap of 4 tracked files was used to prove
non-vacuity, entirely inside the leased `.herd/pool/pool-01` surface.

## Environment limits

Same environment limit as prior FAC-681 rounds: this sandbox's git is 2.34.1,
below the 2.38 floor the merge-tree machinery needs, so
`TestProveEquivalentLandedContextChangedStack` and
`TestPipelineContract_ConflictEvidenceIsRebaseNeeded` cannot be made to pass
here regardless of which commit is checked out. That is a sandbox capability
gap, not a defect in this candidate.
