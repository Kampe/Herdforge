sha: d800066db98e95cca5ee424c2d3d8bdb8cc34ac7
branch: fac-630-admission-diag
task: FAC-630
reviewer: assayer
reviewer-family: openai
builder-family: anthropic
verdict: PASS
reviewed-head: d800066db98e95cca5ee424c2d3d8bdb8cc34ac7
---
No blocking findings. This candidate changes only `pkg/reviewledger/admission_diagnostic_test.go`, tightening `TestRefusalNamesANonIndependentReviewerFamily` so the old generic refusal can no longer satisfy the assertion. Code-review-graph 2.3.7 indexed the exact candidate (16,455 nodes, 1,390 files) and classified the one-file test change as risk 0.05 with no affected flows or test gaps; direct diff inspection confirmed no production change.

Evidence:

1. `go test ./pkg/reviewledger -run '^TestRefusalNamesANonIndependentReviewerFamily$' -count=1` passed at the reviewed SHA. `go test ./pkg/reviewledger -run '^TestRefusal' -count=1` and `go test ./pkg/reviewledger -count=1` also passed. `git diff --check HEAD^ HEAD` passed.
2. Non-vacuity was observed red then green in the isolated pool. After confirming the Git toplevel was under `.herd/pool/`, I restored only the worktree copy of `pkg/reviewledger/admission.go` from `8981f941f6c2f47e3b70c1c8e76dc110fef0d1a0`, the exact parent of the FAC-630 production commit. The focused test exited 1 on its intended assertion and reported the old message, `no independent PASS verdict with durable verification evidence`. Restoring the candidate blob produced the expected HEAD SHA-256 and the focused test passed again.
3. `make all` passed preflight, build, nested contract tests, and the FAC-151 hermetic compile. Its full shuffled unit run kept `pkg/reviewledger` green but reported unrelated failures in `pkg/harness`, `pkg/herdr`, `pkg/launch`, `pkg/review`, `pkg/sync`, and `pkg/verifier`; none are touched or structurally consumed by this assertion-only diff.
4. `make lint all` passed vet, nested-module vet, the FAC-215 hermeticity scan, and package inventory before the security gate hit a host prerequisite failure. The installed `gosec` command is a mise shim with no configured version, so it produced no report and the gate's subsequent `jq` read a null `Issues` field. This is an environment limitation, not contradictory evidence for the changed reviewledger test.

Residual risk is limited to the disclosed repository-wide gates that could not complete cleanly on this host. The scoped package behavior and the required mutation/non-vacuity property were both directly verified against the immutable candidate.
