sha: 7d7f2443291bf93deeaca0b6706a22327a48f9c9
branch: herd/fac-662
task: FAC-662
reviewer: pool-01
reviewer-family: anthropic
builder-family: openai
verdict: FAIL
reviewed-head: 7d7f2443291bf93deeaca0b6706a22327a48f9c9
---

Note: docs/prompts/review-contract.md, which the dispatch packet instructed me
to read, does not exist anywhere in this pool worktree, the canonical repo, or
the review-surfaces tree. Reviewed against the diff and existing test suite
instead.

Reviewed in a clean worktree pinned at 7d7f2443291b (git rev-parse
--show-toplevel resolves under .herd/pool/pool-01, confirming isolation from
the shared checkout at /home/kampe/Projects/Herdforge). `go build ./...` and
`go vet ./...` are clean.

`go test ./cmd/herd/...` FAILS on this candidate:

    --- FAIL: TestReceiptIssueCLI_AdmitsEveryIsolatedAgentClass (0.47s)
        review_cli_test.go:1550: recovery admission failed: exit status 1
        herd receipt: recovery receipt: candidate supersession: checkout is not
        registered by git worktree

This is a pre-existing test (not touched by this diff) whose own doc comment
on runReceiptIssue (cmd/herd/main.go:10082-10089) describes it as "the
coordinator's explicit ADMISSION command for every isolated agent class ...
verification gates, recovery sentinels, and harvest/integration owners." The
candidate changes runReceiptIssue so that ANY `--role recovery` invocation is
now unconditionally routed through the new `authenticatedRecoveryIdentity`
(cmd/herd/receipt_recovery.go), which requires: the target directory be a
git-registered worktree (`git worktree list --porcelain` must list it), a
prior signed TASK-CONTEXT.json exists there with role worker/recovery, and a
long chain of exact identity/reachability checks against it.

That is the right authority chain for the new candidate-supersession use case
(FAC-631/FAC-662: a coordinator re-issuing a receipt bound to a closed
worker's authenticated base so a superseding candidate can be dispatched), and
the new unit tests for that path (receipt_recovery_test.go,
shot_supersession_test.go, supersession_test.go) all pass and are non-vacuous
— I confirmed this by reverting cmd/herd/shot_task.go to its parent-commit
blob inside this isolated pool worktree, which makes
TestRecordShotLifecycleLeaseDrivesRecoveringSupersessionPath fail with "shot:
lifecycle candidate ... conflicts with reported ...", then restored the
candidate file (`git status --porcelain` clean afterward) and reran to
confirm it passes again.

But "recovery" is also the pre-existing, distinct role used for
recovery-sentinel agents (pkg/router/launch.go, pkg/eligibility/eligibility.go,
docs/agent/lane-registry.json) — lightweight, non-mutating agents that inspect
a workspace to detect stranded work, per pkg/dispatch/review_owner.go:27
("recovery-sentinel lanes detect stranded work and report it; they do not
service queues"). Nothing about that role implies the workspace already holds
a signed worker/recovery receipt for the exact task, or that it is registered
as a git worktree via the coordinator's `git worktree add`. The candidate has
conflated the two meanings of "recovery" under one flag value and hard-routed
both through the heavyweight, supersession-specific identity chain, breaking
the receipt-issuance path the recovery-sentinel class actually relies on. The
failure above is real, reproducible, and not a test-fixture artifact of my
review setup — the fixture (`writeSignedReceipt` + `approveFixture`) matches
how this same test suite already exercised the (previously working) verifier
and integration roles side by side with recovery in the same loop.

Fail-closed as required (CLAUDE.md invariant #2): `go test ./cmd/herd/...`
does not pass, so this candidate cannot land as-is. The lifecycle/dispatch
supersession machinery itself (pkg/lifecycle/supersession.go,
cmd/herd/shot_supersession.go) is well-fenced — CAS on sequence/lease/branch/
candidate, idempotent replay, rollback on partial failure, evidence carried
forward without transferring stale verification/review/approval readiness —
but the receipt_recovery.go integration needs to gate on more than
`role == recovery` (e.g. only take the authenticated-identity path when a
prior receipt is actually present / an explicit supersession flag is passed)
so it doesn't also swallow the recovery-sentinel admission case.

go test ./pkg/lifecycle/... : ok
go test ./pkg/dispatch/... : ok
go test ./cmd/herd/... : FAIL (1 of many tests, see above)
