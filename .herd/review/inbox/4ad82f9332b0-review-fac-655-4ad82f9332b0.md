sha: 4ad82f9332b021abe17b0ea20b0ccf42a889f96e
branch: herd/fac-655
task: FAC-655
reviewer: review-fac-655-4ad82f9332b0
reviewer-family: openai
builder-family: xai
verdict: FAIL
reviewed-base: 4d9bd4aa0464755608f97c0c8ffa189e9bbb8be3
reviewed-head: 4ad82f9332b021abe17b0ea20b0ccf42a889f96e
---

FAIL: the selector/task identity split is corrected for ordinary refs, but one valid closeable-card form remains refused.

1. [High] Zero-padded branch refs are normalized differently from provider card refs. `reviewTaskSelectorRef` in `cmd/herd/review_identity.go:76-81` delegates branch extraction to `drainCandidateRef`, whose `hsync.NormalizeRef` call in `cmd/herd/drainadapt.go:441-446` turns `herd/fac-018` into `FAC-18`. The active-list match at `cmd/herd/review_identity.go:32-40` canonicalizes the provider task with `reviewledger.CloseableCardRef`, which preserves the padding (`FAC-018`), so the valid branch/card pair cannot match. The fallback then asks `GetTask` for `FAC-18`, which can also miss a provider card keyed as `FAC-018`. This blocks packet creation for a valid candidate and violates the new branch-to-closeable-card binding contract. The selector and provider ref need one shared canonicalization rule before comparison and lookup.

The launch-reviewer/card-task separation itself is correct at the reviewed head: the packet task is canonicalized by `reviewPacketTaskIdentity`, while ledger `Reviewer` is derived from the original selector through `reviewLaunchRecordBinding`. The prior FAC-655 regression was reproduced as a mutation and the current test caught it.

## Tests run
- `code-review-graph build --repo <isolated pool root>` — PASS; rebuilt graph at candidate `4ad82f9332b021abe17b0ea20b0ccf42a889f96e` (9,232 nodes, 121,474 edges, 753 files).
- `env -u GOROOT go test ./cmd/herd -run 'Test(ResolveReviewTaskRef|ReviewPacket|RecordAssertedBuilderLaunch|ReviewLaunchRecordBinding|ReviewAgentName|ShortRef|TabLabel|ReviewPool|ResolvePoolCandidate|CandidateSurface|NoSurface|DirtySharedCheckout|IntentionalDirtyState|UnreadableStatus|EveryPoolParsingPass|PoolOptions|ModelRequires|LaunchFlags|ReviewerReadiness|Preflight)' -count=1` — PASS.
- `env -u GOROOT go test ./pkg/reviewledger -run 'Test(CloseableCardRef|RequireCloseableCardRef|CompleteLaunchProvenance|EnsureRecord)' -count=1` — PASS.
- `env -u GOROOT go build ./cmd/herd` — PASS; ignored build artifact moved out of the pool afterward.
- `env -u GOROOT go test -race ./cmd/herd -run 'Test(ResolveReviewTaskRef|ReviewPacket|RecordAssertedBuilderLaunch|ReviewLaunchRecordBinding)' -count=1` — PASS.
- `env -u GOROOT go test -race ./pkg/reviewledger -run 'Test(CloseableCardRef|RequireCloseableCardRef|CompleteLaunchProvenance|EnsureRecord)' -count=1` — PASS.
- `env -u GOROOT go run ./scripts/hermeticity/` — PASS.
- Mutated old FAC-655 reviewer binding in `recordAssertedBuilderLaunch`, then `env -u GOROOT go test ./cmd/herd -run '^TestRecordAssertedBuilderLaunchBindsLaunchReviewerAndCloseableTask$' -count=1` — RED, exit 1 with the card-derived reviewer mismatch; restored the exact candidate and reran the test — GREEN.
- `env -u GOROOT go test ./pkg/provider -run '^TestGitHubProvider_ListTasks_InReviewUnsupported$' -count=1` — PASS; adapter behavior was inspected as a compatibility limitation, not used as the verdict finding.

Residual risk: no live provider, board, claim, auth, pane, or other-host mutation was performed. Full repository CI and managed gates were not rerun per packet scope. The isolated review worktree is clean and remains at the exact reviewed head.
