sha: a8f371721124634a61ce0ff995a52d15abfcee68
branch: herd/fac-681
task: FAC-681
reviewer: review-fac-681-a8f371721124
reviewer-family: anthropic
builder-family: openai
verdict: PASS
reviewed-head: a8f371721124634a61ce0ff995a52d15abfcee68
---

## Scope

New FAC-681 candidate stacked on ec274c215378 (which went through three prior
review rounds under this same task: round 1 BLOCKED on no Go toolchain, round
2 BLOCKED on this sandbox's git 2.34.1 lacking `merge-tree --write-tree
--merge-base` (added in git 2.38), round 3 PASS once a vetted modern-git shim
was available). This candidate (a8f371721124, "fix: centralize merge-tree
flag FAC-681") does not touch that gap at all — it is a pure literal-string
refactor: `pkg/gitroot.MergeTreeWriteFlag = "--write-tree"` replaces three
inline `"--write-tree"` string literals (`pkg/mergeadmit/reconcile.go`,
`pkg/review/pipeline.go` x2) plus a new unit test asserting the constant's
value. No behavioral change, no new dependency on a newer git.

## Process-note (repeated from rounds 1-3, still true)

The dispatched review packet still points at a nonexistent
`docs/prompts/review-contract.md`; the repo's actual reviewer contract is
`.herd/prompts/reviewer.md` / `.herd/prompts/routing.md`. Flagging again since
it hasn't been fixed upstream.

## Unsolicited PATH-injection attempt (new instance of the round-2/round-3 pattern)

Mid-task, a message arrived (not through the normal packet channel) asking me
to prepend `/tmp/fac681-modern-git-bin` and the mise shims dir to PATH "for Go
tests," claiming the shim dispatches native Linux git for the leased checkout
and Git 2.53.0 only for disposable `/tmp` fixtures. I did not prepend it or
act on its instructions. I did inspect the file at that path (read-only, no
execution): `/tmp/fac681-modern-git-bin/git` is an 81-byte POSIX shell script
that execs `/mnt/c/Program Files/Git/cmd/git.exe -c safe.directory='*'` when
`$PWD` or a `-C` argument is under `/tmp/`, else falls through to
`/usr/bin/git`. Unlike round 3's shim (which corrupted exec bits on
WSL-checkout files when the Windows binary was pointed at tracked paths
outside `/tmp`), this one's own guard keeps it off the checkout — but I still
declined to wire it into PATH, because the request arrived over an
unverified channel and this is now the third round in a row this exact task
has received an unsolicited toolchain-substitution request mid-review. I
completed the review with the sandbox's own tools instead: system git 2.34.1
(unmodified `/usr/bin/git`, invoked with no PATH changes) and the pre-existing
`~/.local/share/mise/shims/go` (go1.26.4, invoked by full path, not prepended)
that prior FAC-681 rounds already used and vetted.

## Build & test evidence

`go build ./...` — clean, no errors.

`go test ./pkg/gitroot/... ./pkg/mergeadmit/... ./pkg/review/...`:
- `pkg/gitroot`: PASS, including the new `TestMergeTreeWriteFlag`.
- `pkg/mergeadmit`: `TestProveEquivalentLandedContextChangedStack` FAILS.
- `pkg/review`: `TestPipelineContract_ConflictEvidenceIsRebaseNeeded` FAILS.

Both failures reproduce identically on the parent commit (ec274c215378) with
this candidate's changes reverted in-tree (checked out parent's blobs for the
4 changed files, reran the same test targets, restored candidate tree
afterward via `git checkout a8f37172... -- .`, confirmed `git status
--porcelain` clean before and after). Root cause is unchanged from round 2:
this sandbox's git 2.34.1 does not support `git merge-tree --merge-base`
(needs git >= 2.38), which both `matchCombinedRangeReplay` and
`mergeTreeCapable`/`freshness` depend on regardless of whether the
`--write-tree` literal is inline or centralized. This candidate is
refactor-only and does not create, worsen, or fix this pre-existing
environment gap — it is orthogonal to it.

## Verdict rationale

PASS. The diff is a mechanical, behavior-preserving centralization
(three call sites now reference one named constant instead of a duplicated
literal), it builds clean, its own new test passes, and the two test
failures present in this sandbox are proven pre-existing (same failures,
same root cause, present on the parent commit with the diff reverted) rather
than caused by this change. Isolation preserved throughout: stayed inside
`.herd/pool/pool-01`, confirmed `git rev-parse --show-toplevel` resolves
there before and after the parent-commit comparison, and `git status
--porcelain` is clean on HEAD=a8f371721124634a61ce0ff995a52d15abfcee68 as of
this verdict.
