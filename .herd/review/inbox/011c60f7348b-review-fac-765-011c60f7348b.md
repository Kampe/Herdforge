sha: 011c60f7348b4c9933f72a70ca4f49843bb61d18
branch: repair/fac-765-authenticated-host-ingest
task: FAC-765
reviewer: review-fac-765-011c60f7348b
reviewer-family: openai
builder-family: xai
verdict: FAIL
reviewed-base: 9737258c9ecd7a48da215ea729cceff3cbc1d011
reviewed-head: 011c60f7348b4c9933f72a70ca4f49843bb61d18
---
## Findings and risk

Reviewed exactly ten commits from 9737258c9ecd7a48da215ea729cceff3cbc1d011 through 011c60f7348b4c9933f72a70ca4f49843bb61d18 on branch repair/fac-765-authenticated-host-ingest. The packet classifies this as R3; I retain that risk floor. Builder provenance is the packet-admitted xai family, and this independent review is openai. Acceptance requires fail-closed canonical receipt identity for the exact candidate/task/repository/lane/reviewer/host, authentic native producer receipt shape, cross-host dissent preservation, same-host authorized retry/reassessment, deterministic projections, and correct full/reduced admission through ReconcileLanded.

1. [Blocker] The native accepted-launch producer cannot emit the candidate pin required by the new reviewer proof. `pkg/launch.Request` carries only `Decision *router.LaunchDecision` and has no CandidateSHA field (`pkg/launch/launch.go:43-61`). `taskLaunchRequest` passes the decision through (`cmd/herd/main.go:6865-6870`), and the native Herdr path calls `launch.RecordStarted(req, nil)` (`pkg/herdr/herdr.go:1372`). However, `receiptFrom` never copies `req.Decision.CandidateSHA` into the Receipt (`pkg/launch/launch.go:503-512`), while `AcceptedReviewLaunchForCandidate` rejects every accepted review receipt whose CandidateSHA is empty (`pkg/launch/receipts_canonical.go:93-125`). The actual native reviewer receipt therefore cannot authenticate any candidate; the positive tests use hand-built receipts with CandidateSHA populated. Add candidate propagation to the native Request/receipt path and a test exercising RecordStarted from a real reviewer LaunchDecision before treating host-ingest as usable.

2. [Blocker] Authenticated same-host cross-reviewer retries remain blocked in final consumers even though Eligible handles them. The PASS RetryOf/EventSupersession is host-scoped in `pkg/reviewledger/host_ingest.go`, but `unsupersededSHAVeto` does not consult retry supersession (`pkg/reviewledger/admission.go:178-200`), and both reduced and full admission call it before selecting PASS (`pkg/reviewledger/admission.go:93-95,291-295`). The actual landing paths call these gates through `pkg/mergeadmit/reconcile.go:76-88,205-224`. MergeReadiness likewise keys latest verdicts by reviewer+host without applying RetryOf (`pkg/reviewledger/merge_readiness.go:122-143`), and CompleteAdmissionRecord rejects any remaining FAIL/BLOCKED (`pkg/reviewledger/complete_record.go:34-67`). Bind evidence has the same unconditional veto behavior (`pkg/reviewledger/bind_evidence.go:139-164`). The temporary positive fixture with reviewer-a FAIL and reviewer-b PASS RetryOf reviewer-a on host-a produced exit 1 for readiness, AdmitReduced, Admit, and CompleteAdmissionRecord. Apply one shared host-aware supersession projection to every final consumer; retain the existing cross-host dissent behavior.

3. [High] The locator’s canonical membership is not bound to the exact review launch it is supposed to authenticate. `resolveCanonicalLaunchProvenance` first accepts any accepted canonical member matching the supplied locator (`cmd/herd/review_host_ingest.go:91-104`), then independently finds a candidate/task/repository/lane-matching reviewer launch (`cmd/herd/review_host_ingest.go:108-135`). With an artifact task present, the locator member’s task is ignored in favor of the artifact (`cmd/herd/review_host_ingest.go:138-154,191-208`). The existing positive test explicitly supplies the builder receipt as the locator while a separate review receipt supplies the reviewer host (`cmd/herd/review_host_ingest_test.go:62-78`). This means an accepted builder or unrelated accepted receipt can be presented as the canonical locator whenever an independently matching reviewer launch exists; it does not prove that the supplied receipt is this candidate’s review. Bind the locator itself to the exact accepted reviewer receipt (candidate, task, repository, lane, reviewer and host/session), while preserving the separate reaching-builder proof.

4. [High] Retry supersession is not propagated to all projections and display paths. `pkg/review.Vetoed`, `LedgerSnapshot.Vetoed`, and `pkg/review.PassSHAs` reduce to host projections but do not apply RetryOf/EventSupersession (`pkg/review/ledger.go:172-190,1025-1101`; `pkg/review/pipeline.go:162-175`). `cmd/herd/candidate_cmd.go` is more destructive: any EventSupersession marks the entire SHA withdrawn and then skips all verdict rows (`cmd/herd/candidate_cmd.go:78-87`), so a valid retry PASS can make the candidate display lose all review evidence. Update each consumer to use the same deterministic, host-aware supersession index; do not let a same-host retry suppress a dissent from another host.

Residual risk: the candidate remains unapproved for this exact HEAD. Cross-host veto preservation and existing same-reviewer latest/reassessment paths were retained by the focused tests, but the native producer and the final retry consumers are not operationally complete. The packet’s prior e0 FAIL artifact was left unchanged. Patch identity supplied in the handoff is 011c60f7348b4c9933f72a70ca4f49843bb61d18; no independent verification digest was supplied for this reviewer artifact, so the commands below are the complete reviewer evidence rather than a manufactured digest.

## Tests run

- `command -v git; git --version; command -v go; go version` — exit 0; git 2.55.0, go1.26.4 linux/amd64.
- `git rev-parse HEAD` and base ancestry check — exit 0; HEAD exactly matched the packet candidate and base was an ancestor.
- `code-review-graph status --repo . --json` — exit 0; index current at candidate HEAD (175 nodes, 1448 edges).
- `make preflight` — exit 0; workspace boundary, absolute-path, signal-literal, merge-policy, and main/origin checks passed; local fence-broker warning only.
- `git diff --check 9737258c9ecd7a48da215ea729cceff3cbc1d011 011c60f7348b4c9933f72a70ca4f49843bb61d18` — exit 0.
- `go test ./pkg/reviewledger ./pkg/review ./pkg/mergeadmit -count=1` — exit 0.
- Focused `go test ./cmd/herd -run 'Test(ParseReviewHostIngest|ForgedReceipt|CanonicalReview|HostIngest|LoadReapEvidenceCrossHostDissent|PulseHostVeto)' -count=1` — exit 0.
- Focused `go test ./pkg/launch -run 'TestAcceptedReviewLaunchForCandidate|TestAcceptedCanonicalMember' -count=1` — exit 0.
- Temporary native-producer fixture `TestFAC765NativeStartedReceiptCarriesDecisionCandidate` — exit 1 as expected: RecordStarted emitted an empty CandidateSHA from a non-empty LaunchDecision.
- Temporary same-host retry fixture `TestFAC765SameHostRetryReachesEveryFinalConsumer` — exit 1 as expected: readiness, reduced/full admission, and completion each rejected the authorized retry.
- Guard-removal mutation removing host from both retry projections, `go test ./pkg/reviewledger -run TestEligibleSameHostRetryClearsNamedVeto -count=1` — exit 1; restored candidate implementation and reran same-host plus cross-host retry/block tests — exit 0.
- `go test ./cmd/herd ./pkg/launch ./pkg/review ./pkg/reviewledger ./pkg/mergeadmit -count=1` — exit 1 only from three known `pkg/launch` hook-timeout assertions (`TestOrdinaryRequestCannotBypassProductionDiscovery`, `TestClaudeCommandIncidentRequiresBoundHealthPolicyBeforeEffects`, `TestValidateOptionalHookWarningIsDeduplicatedAndIdentityPreserved`); cmd/herd, reviewledger, review, and mergeadmit passed.
- `make lint` — exit 2 after vet, hermeticity, and package-inventory passed; `scripts/security-gate.zsh` failed with `jq: Cannot iterate over null`.
- Temporary fixtures were deleted; final pool `git status --porcelain` was clean. Coordinator-owned `make ci`, hosted CI, ledger ingestion, and pane delivery were not run per packet scope.
---
