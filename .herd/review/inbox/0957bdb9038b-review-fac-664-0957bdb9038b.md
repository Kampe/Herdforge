sha: 0957bdb9038b0fa0544c9473b6c1cfe8d4d36b7c
branch: herd/fac-664
task: FAC-664
reviewer: pool-01
reviewer-family: anthropic
builder-family: openai
verdict: PASS
reviewed-head: 0957bdb9038b0fa0544c9473b6c1cfe8d4d36b7c
---
Reviewed candidate 0957bdb9038b0fa0544c9473b6c1cfe8d4d36b7c (test: make
lifecycle argv assertion semantic, FAC-664) in a clean worktree pinned at that
exact SHA (git rev-parse --show-toplevel resolves under .herd/pool/pool-01,
confirming isolation from the shared checkout). Diff is a single-line change
to one test file: pkg/lifecycle/lifecycle_bin_test.go, in
TestKaneoListArgsPreservesRepositoryWrapperShape. It widens the `hasFields`
scan from `got[1:]` to the full `got` slice, while the preceding line
separately still asserts `got[0] != "list"` as a Fatal precondition.

Traced against the implementation in pkg/lifecycle/lifecycle.go
(kaneoListArgs, ~line 260): in the legacy-wrapper branch under test, the
returned slice is exactly `["list", "--fields", "ref,title,...,merged_at"]`.
Since index 0 is independently pinned to "list" by the line directly above
the loop, scanning from index 0 vs index 1 cannot change whether "--fields"
is found — "list" never equals "--fields" — so this is a behavior-preserving,
purely mechanical widening, not a vacuity fix or a bug fix. It follows on
directly from parent commit 2f147806d ("avoid positional argv assertion"),
which is the commit that actually removed positional coupling; this candidate
just makes the remaining scan bound (`[1:]` vs full) match the stated intent
of "search the complete wrapper argv" without weakening the got[0]=="list"
check that stays separate. Commit message accurately describes the change;
no scope creep, no production code touched.

Risk tier: R0 (test-only, single-line, mechanical, no behavior change to
non-test code). No go toolchain is available on this review host to execute
`go test`, so correctness was verified by manual trace of the loop bounds
against the concrete return value of kaneoListArgs() for the wrapper-shape
branch, cross-checked against both call sites of the assertion (got[0] and
the hasFields loop) — the change is provably a no-op over the current
implementation and does not introduce or hide a false-positive test.

No blocking finding. PASS.
