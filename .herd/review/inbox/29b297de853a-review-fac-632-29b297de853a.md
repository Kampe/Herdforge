sha: 29b297de853a004223423e8e05e315fdddfbad0e
branch: fac-632-approve-alarm
task: FAC-632
reviewer: review-fac-632-29b297de853a
reviewer-family: openai
builder-family: google
verdict: FAIL
reviewed-head: 29b297de853a004223423e8e05e315fdddfbad0e
---

## Finding

- **[High] The new test does not prove that the alarm fires for a suppressed-only sweep.** In `cmd/herd/review_cli_test.go:1969-1975`, the test asserts only a non-zero exit and the generic `CONTROL-PLANE STALL:` text. With the production tombstone-suppression condition deliberately disabled, the unchanged candidate test still passed: the missing completion and launch receipts fell through to the refused path, which emitted the same alarm. Thus the test can pass with `suppressed=0` and does not establish FAC-632's claimed regression boundary. This is a test-only candidate, so the missing state-specific assertion leaves its task unproven and violates the repository's non-vacuity discipline.

## Evidence

- Isolated review root: `.herd/pool/pool-01`; candidate and reviewed HEAD both `29b297de853a004223423e8e05e315fdddfbad0e`; worktree clean before verdict.
- Comparison base: `5ba3eff7a0cfce492824124db01589177e3c56d1`; sole changed path is `cmd/herd/review_cli_test.go`.
- Code-review-graph 2.3.7 was refreshed at the candidate: 16,465 nodes, 204,666 edges, 1,391 files. `detect-changes` reported one changed test function, no affected flows, no test gaps, and risk score 0.05; direct source inspection confirmed the relevant production branches in `cmd/herd/main.go`.
- Baseline: `go test ./cmd/herd -run '^TestApproveCLI_StallAlarmFiresOnSuppressed$' -count=1 -v` passed. Both alarm tests also passed five consecutive runs.
- Positive mutation check: removing `suppressed` from `stalled := failed + refused + suppressed` made the new test fail, as expected.
- Decisive non-vacuity mutation: disabling the tombstone suppression branch while leaving the candidate test unchanged still produced PASS. The mutation was restored immediately and `git status --porcelain` was clean.
- `make all` passed preflight, build, nested contract tests, hermetic-profile compilation, and the changed `cmd/herd` package, but the repository-wide shuffled suite failed in unrelated unchanged packages (`pkg/harness`, `pkg/herdr`, `pkg/launch`, `pkg/review`, `pkg/security`, `pkg/sync`, and `pkg/verifier`). Focused reruns reproduced all except `pkg/security`; none are in the candidate diff.
- `make lint all` stopped before `all` in the unchanged security gate because its `jq` expression attempted to iterate a null aggregate report. Vet, hermeticity, and package-inventory checks had passed.
- The packet-requested `docs/prompts/review-contract.md` is absent from both the candidate tree and `origin/main`; the review followed the packet and repository `AGENTS.md` contract directly.
