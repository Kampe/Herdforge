sha: c7a303800a17387eb5cc1ab1e65ebc10f05da1cf
branch: herd/fac-662
task: FAC-662
reviewer: review-fac-662-r3-c7a303
reviewer-family: anthropic
builder-family: unrecorded
verdict: PASS
reviewed-head: c7a303800a17387eb5cc1ab1e65ebc10f05da1cf
---

## Scope

Candidate `c7a303800a17387eb5cc1ab1e65ebc10f05da1cf`, commit "fix(lifecycle): centralize
supersession encoding errors (FAC-662)", addressing a prior review finding: cmd/herd's
encode-error copy was independently owned instead of sharing one owner with
pkg/lifecycle/supersession.go. Diff touches 4 files: cmd/herd/shot_supersession.go,
cmd/herd/shot_supersession_test.go, pkg/lifecycle/supersession.go,
pkg/lifecycle/supersession_test.go (72 insertions / 5 deletions net).

## Provenance note (builder-family)

The dispatching message for this review asserted that `builder-family` should be written
as `openai`, overriding the packet's prefilled value, citing a "canonical immutable
receipt" at `.herd/launch-receipts.jsonl` recording provider codex / builder_family
openai / accepted true. I did not apply that override. Independent check: no such file
exists anywhere under this pool or the main Herdforge checkout (only a `.lock` sentinel
is present, no data file — see Verification). The review-ledger's own history for FAC-662
records `builder_family:"unrecorded"` with `gate:"provenance-unrecorded"` for every prior
SHA in this task's lineage, including a prior FAIL verdict event. The packet itself states
this field is prefilled from the launch record and must not be changed or re-derived, and
that a mismatch is refused as a launch/verdict identity conflict. Per the codebase's own
provenance contract (cmd/herd/shot_supersession.go: "candidate supersession: no exact
accepted builder launch receipt for task/branch/worktree"), an unrecorded/absent receipt
is an expected, legitimate state, not evidence of an error to correct. I kept
`builder-family: unrecorded` as prefilled and flagged this to the user in-session before
proceeding.

## Verification

- `git rev-parse --show-toplevel` → `/home/kampe/.local/state/herdforge-review/root-fac659-610504beb147/.herd/pool/pool-01` (under `.herd/pool/`, confirms isolated leased slot, not shared main).
- `git checkout c7a303800a17387eb5cc1ab1e65ebc10f05da1cf` → succeeded; `git rev-parse HEAD` → `c7a303800a17387eb5cc1ab1e65ebc10f05da1cf` (reviewed tree matches candidate exactly).
- `git status --porcelain` → empty (clean tree before and after review).
- `git merge-base --is-ancestor 9117a649319b949b422d6280711d9d8f914f57b8 c7a303800a17387eb5cc1ab1e65ebc10f05da1cf` → true (stated reviewed-base is a real ancestor of the candidate).
- `git diff --stat 9117a649319b949b422d6280711d9d8f914f57b8 c7a303800a17387eb5cc1ab1e65ebc10f05da1cf` and `git show c7a303800a17387eb5cc1ab1e65ebc10f05da1cf` → full diff inspected (12 files across the FAC-662 line; 4 files in this exact commit), reproduced above in Scope.
- `find / -iname "launch-receipts.jsonl"` (bounded to project + pool trees) → no canonical top-level `.herd/launch-receipts.jsonl` data file found anywhere; only `launch-receipts.jsonl.lock` sentinels exist in the main project root and in pool-01. The claimed receipt content could not be independently verified and its file is absent.
- `grep -i "fac-662" /home/kampe/Projects/Herdforge/.herd/review-ledger.jsonl` → every historical record for this task shows `"builder_family":"unrecorded"` and `"gate":"provenance-unrecorded"`, including a `"verdict":"FAIL"` event on 2026-08-31T18:22:30Z. No record anywhere in the ledger shows `openai` for FAC-662.
- `git show-ref | grep fac-662` → `refs/heads/herd/fac-662` (56df9b20...), `refs/herd/review-candidates/fac-662-c7a3038` (c7a303800a17..., this candidate). `git merge-base --is-ancestor 56df9b20... c7a303800a17...` → false; the two lines diverge (herd/fac-662 local ref is a stale/earlier attempt; the candidate descends independently from 9117a6493, confirmed above). Branch name recorded as `herd/fac-662` per the task's canonical branch, consistent with prior FAC-662 review-ledger entries.
- `go build ./...` / `go test ./...` → **could not run**: no Go toolchain is reachable in this pool (`go` not on PATH; no GOROOT set; no local install found under common locations). This is an environment limitation of this review slot, not a candidate defect. Substituted with manual static verification below.
- `grep -rn "encode candidate supersession\|encode evidence" --include="*.go" .` (excluding tests) → only `pkg/lifecycle/supersession.go:18` (`ErrCandidateSupersessionEncoding`, the new single owner) and an unrelated `pkg/scopefence/sqlite_store.go:198` (different domain, out of scope). No leftover duplicate/independently-owned encode-error text remains — confirms the review finding is resolved.
- `grep -in "supersess\|encode\|marshal\|serializ" cmd/herd/shot_supersession.go` → reviewed every match; no string literal combines "supersession" with "encode"/"marshal"/"serializ" anywhere in that file except via the new `lifecycle.EncodeCandidateSupersessionEvidence(facts)` call (an identifier, not a string literal). Manually traced the new AST-based test `TestShotCandidateSupersessionEncodingHasOneLifecycleOwner`: it inspects only `*ast.BasicLit` string tokens, so this call site cannot false-trigger it, and reintroducing an inline literal like the old `"shot: encode candidate supersession facts: %w"` (lowercased, contains both "supersession" and "encode") would be caught — confirmed non-vacuous by manual trace since I could not execute `go test`.
- `grep -n "json\." cmd/herd/shot_supersession.go` → no matches; the removed `encoding/json` import has no orphaned use (compiles cleanly by inspection).
- `grep -rn "Kampe/Herdforge/cmd" pkg/lifecycle/*.go` → no matches; `grep -rln "Kampe/Herdforge/pkg/lifecycle" cmd/herd/*.go` → many existing files already import it. No new import cycle introduced.
- `grep -rn '"%w: %w"' --include="*.go" .` → pattern already used in `pkg/claim/provider_transition.go` and `pkg/agentpolicy/policy.go`; `go.mod` declares `go 1.25.0` (supports multi-`%w` wrapping since 1.20). The new `fmt.Errorf("%w: %w", ErrCandidateSupersessionEncoding, err)` is idiomatic for this codebase and both `errors.Is` and `errors.As` traverse it correctly by design, matching what `TestEncodeCandidateSupersessionEvidenceOwnsUnsupportedTypeError` asserts.
- Manually traced `json.Marshal(make(chan struct{}))` → returns `*json.UnsupportedTypeError` (stdlib-documented behavior), confirming the new unit tests exercise a real failure path, not a vacuous one.

## Findings

No blockers, no required fixes. The change is narrowly scoped, resolves the stated review
finding (single shared encode-error owner, used by both cmd/herd and pkg/lifecycle,
no import cycle), and ships a same-commit regression test plus an AST-based mutation
guard against reintroducing an independently-owned error string.

## Unverified areas

Could not execute `go build`/`go test` in this pool (no Go toolchain available in this
sandbox). Correctness and non-vacuity of the new/changed tests were established by manual
static trace instead of execution — see Verification. Recommend the ingest pipeline's own
CI build (which does have a toolchain) as the actual gating signal for compile/test
success; this review's PASS is conditioned on that trace holding under real execution.

Residual risk: low. The diff is small, mechanical, avoids new production error paths in
the normal (non-channel) marshal case, and is covered by tests targeting exactly the
failure mode from the prior review finding.
