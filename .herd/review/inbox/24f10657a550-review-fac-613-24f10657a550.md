sha: 24f10657a550f601e6bd51a319ff1cf12b257218
branch: recovery/idle-census-final-1582
task: FAC-613
reviewer: review-fac-613-24f10657a550
reviewer-family: open-weight
builder-family: anthropic
verdict: PASS
reviewed-base: 91f2e712b72266c6246a84a21e58be6f48f9b7bf
reviewed-head: 24f10657a550f601e6bd51a319ff1cf12b257218
---
## Findings and risk

Exact reviewed range: 91f2e712b72266c6246a84a21e58be6f48f9b7bf..24f10657a550f601e6bd51a319ff1cf12b257218 (8 commits: d8365893, 2b668fc3, f8458712, 0cc1ab5e, 763ae566, 5be6a9af, 5cb79a91, 24f10657), diff stat 12 files, +2185/-25. Risk tier R3 (destructive core infrastructure: worktree GC/reaper). Builder family anthropic; this reviewer is OpenCode on litellm/lazer/glm-5.3-flash, normalized family open-weight — different family from the builder, satisfying the R3 cross-model gate. patch_id: 57b634abbdebdbe53117f1c9907be613275fe255. verification_digest: sha256:bb62c1758d8026db81e25e496ff65aa3d6ceeceeb8b30796a2fb01eaa8bcd2c5. full_range: 91f2e712b72266c6246a84a21e58be6f48f9b7bf..24f10657a550f601e6bd51a319ff1cf12b257218 (digest identifies coordinator retained evidence .worktrees/orchestrator/.herd/coordinator-resume/completion-recovery-20260909/fac613-review-evidence-1604.json).

Independent verification at the exact pin (worktree at .herd/pool-fac679-haiku-2331/pool-01, toplevel confirmed inside the leased pool slot, HEAD = sha, status clean before and after every mutation):

1. **GC fail-closed identity (d8365893, 763ae566)** — `Pool.GC` (pkg/worktree/pool.go:494) now verifies the complete candidate set (`verifyGCSet`) before any removal, re-verifies each slot immediately before its own destructive step, and pins identity (normalized path, registered git HEAD, `os.SameFile` inode identity) between passes. `git worktree remove` runs without `--force`; post-removal, a replaced or persisting path is refused and preserved. `containedRepoPath` rejects symlinks at the leaf, resolves ancestor symlinks (macOS /var), and refuses anything escaping the pool root; `registeredWorktrees` accepts only paths in git's own `worktree list --porcelain` registry; `gitFullyClean` includes `--ignored`; `verifyReachableFromBase` refuses HEADs not ancestors of base. All proven by `TestPoolGCRefusesUnregisteredDirectory`, `TestPoolGCRefusesPathOutsidePoolRoot`, `TestPoolGCRefusesSymlinkSlot`, `TestPoolGCRefusesDirtyUntrackedContent`, `TestPoolGCRefusesIgnoredContent`, `TestPoolGCRefusesHeadNotReachableFromBase`, `TestPoolGCRefusesWhileAnySlotLeased`, plus a success path proving Ensure can rebuild after GC. `TestPoolGCRefusesIgnoredContent` explicitly proves `gitClean` (Lease-grade) passes ignored-only content while GC's stricter check refuses it — the guard does real, additional work, not a re-test of an existing check.
2. **Bounded discovery, cursor fairness, dry-run purity (2b668fc3, 24f10657)** — `runIdlePoolTick` (pkg/worktree/idle_discovery.go:350) bounds per tick by MaxRoots (default 10) and MaxElapsed (default 30s) applied during inspection, rotates candidates via `fairOrder` so no root starves, and writes the cursor only on `act`. Dry-run purity is proven by `TestDiscoverIdlePools_DryRunWritesNoFiles` (no new `.herd` entries, no cursor, pool untouched) and the real-CLI test asserts the same against the built binary. The canceled-context test (new at this head) proves a pre-canceled context surfaces as retained/failed, never eligible, with nothing mutated — and its non-vacuity is load-bearing (the builder's own RED proof disconnected the deadline from the caller context and watched it fail; I re-verified the test GREEN at the pin).
3. **Protection surfaces** — retirement-manifest pools are retained while any manifested generation lacks a matching `complete` phase record and released only when all are complete (`TestDiscoverIdlePools_ActiveUncompletedManifestRetainsHeldTerminalReclaims` covers both sides); unparseable manifest/journal lines fail the whole tick closed with nothing mutated (`TestDiscoverIdlePools_UnparseablePhaseJournalFailsClosed`); leased, dirty, and unmerged slots survive untouched (`TestDiscoverIdlePools_RetainsLeasedDirtyAndUnmergedSlots` uses a real committed divergent slot). Discovery enumerates only `.herd/pool-*` directories — the live `.herd/pool` is never a candidate, and nothing outside `.herd` is read or touched.
4. **Process-census semantics (5cb79a91)** — `lsofPositiveExitOne` (pkg/resources/git_census.go:616) accepts lsof exit 1 as owner evidence only when stderr is empty and stdout contains a positive `f`-descriptor (not `rtd`/`txt`/`mem`) for a parsed `p`-line PID; every other exit-1 shape falls through to the refusal path. Both branches are fail-safe for GC (error → refuse; positive evidence → CWD/OpenFile set → refuse); the fix only stops discarding genuine owner evidence. `inspectSlotUse` refuses nil inspector, census error, `MetadataUnavailable`, and any CWD/OpenFile/ReferencedPath, with a 2s probe timeout. Proven by `TestLSOFProcessInspectorPreservesPositiveExitOneOwnerEvidence` and, importantly, `TestPoolGCDefaultCensusRefusesLiveProcessWithOwnerEvidence`, which runs the **default, unmodified** native lsof census against a **real child process** holding cwd + open FD in a real slot and watches GC refuse with "live process owns or references" — not a stub path.
5. **Public command and pulse wiring (f8458712, 5be6a9af)** — `idle-pool` is dry-run by default, `--act` reclaims, exits non-zero on tick error or Failed>0; `pool gc --dry-run` reports REMOVE/RETAIN decisions via `GCPlan`, which runs the identical verification GC uses so a dry run cannot diverge. `runPulseCommandContext` folds a genuine reclaim failure into a non-zero exit (`TestReclaimIdlePoolsOnPulse_GenuineFailurePropagatesNonzero` proves the false return on a schema-corrupt root) while a benign lock-race deferral is not a failure. `TestRunIdlePool_RealCLIDispatch` builds and execs the real binary end to end: unknown-command control, dry-run (no cursor, pool intact), then `--act` (slot reclaimed, cursor persisted). Control-surface manifest bumped to version 30 with the `idle-pool` coordinator-only class and compatibility hash recorded — no silent contract drift.

Independent non-vacuity proofs (this review, both in the leased slot with tracked-file restores verified byte-identical by sha256 and `git status --porcelain` clean afterward):

- Guard A: `lsofPositiveExitOne` forced to `return false` → `TestLSOFProcessInspectorPreservesPositiveExitOneOwnerEvidence` **FAIL** ("positive lsof exit 1 should remain usable owner evidence: exit status 1"), exit 1 → restored (sha256 c007368c… matched pre-mutation) → **PASS**.
- Guard B: `inspectSlotUse` MetadataUnavailable refusal disabled → `TestPoolGCRefusesUnknownProcessCensus` **FAIL** ("GC error = <nil>, want fail-closed census refusal" — GC would have destroyed the slot), exit 1 → restored (sha256 6f2cbdbc… matched) → **PASS**, re-run together with the default-census and canceled-context tests.

Numbered findings (none blocking):

1. **[Low, residual risk for the root-owned Linux gate]** `TestPoolGCDefaultCensusRefusesLiveProcessWithOwnerEvidence` (pkg/worktree/pool_gc_safety_test.go:375) exercises the default native census and depends on real `lsof` without a `LookPath`+`t.Skipf` guard. On a lsof-less Linux CI box the census error would read "process census unavailable" instead of "live process owns or references" and the test would fail rather than skip. This same diff added the lsof guard to the git_census test (git_census_test.go), so the inconsistency is real. The repo's hermeticity checker exits 0 (it keys on test-side `exec.Command`, and the test spawns only `sh`), and macOS local evidence is genuine, so this is not blocking — but the Linux gate root owns may hit it.
2. **[Low, fail-closed liveness]** If GC's per-slot re-verification fails after an earlier slot was already removed, the successful removal's stale record stays in pool.json (state write is skipped on error). A subsequent GC refuses with "slot path does not exist" until `Ensure` rebuilds the directory (pkg/worktree/pool.go:150 recreates missing slots). Fail-closed and self-healing via the documented Ensure path, but GC alone does not converge; acceptable per the card's "serialized or fails closed" requirement.
3. **[Informational]** The pulse wiring hardcodes base `origin/main` and maxRoots 5 (cmd/herd/idlepool.go:99) rather than reading repository config; divergence from a repo's actual base only causes conservative over-retention (retained), never unsafe reclamation. `--base`/`--max-roots` exist on the standalone command.
4. **[Informational]** `isIdlePoolTickBusy` matches the wrapped error text "tick is busy or lock is stale"; a sentinel error would be less brittle. The O_EXCL tick lock has no stale-lock recovery, so a crashed tick requires manual lock removal — fail-closed, operator-visible.
5. **[Informational]** `TestReclaimIdlePoolsOnPulse_ReclaimsCleanRootAndLeavesLeasedAlone` reads the pipe with a single 4096-byte `Read`; a short read could truncate the asserted `eligible=1` line in theory. Test-robustness nit only.

Acceptance-criteria mapping for this range: dry-run/apply idempotence over exact verified targets (met, criterion 1); fail-closed reasons for dirty/untracked, live process, active lease, unresolved candidates, canonical evidence, containment ambiguity (met, criterion 2); concurrent reap/dispatch serialized or fail-closed without partial deletion (met — tick lock + full-set verify + per-slot re-verify, criterion 6); bounded discovery, cursor fairness, no dry-run writes (met, criteria 3/5 in the packet's enumerated scope). Honestly outstanding (operational, not in this delta): exact physical-byte per-path accounting and disk-readback before/after reporting, high/low watermarks, per-lane allocation telemetry, and the two-machine admission fixture are the governor integration pending on main (root-owned, per packet); live shadow inventory/dry-run/recensus rollout and daemon-cadence live observation were not executed by this reviewer (no daemon run; evidence-only review at the pin); pnpm global virtual store is untouched, so that criterion's precondition is not triggered. None of these gaps are claimed as covered by this verdict. Residual risk after this review: low-to-moderate, concentrated in the pending governor wiring on main rather than in this delta.

## Tests run

All commands executed inside the leased pool slot (toplevel verified under .herd/pool-fac679-haiku-2331/pool-01; surface symlink resolution accepted per the isolation contract). All with -count=1, GOMAXPROCS=2, -p=2.

- `git rev-parse --show-toplevel` → /Users/kampe/Personal/Herdforge/.herd/pool-fac679-haiku-2331/pool-01 (isolated, not the shared checkout); `git rev-parse HEAD` → 24f10657a550f601e6bd51a319ff1cf12b257218; `git status --porcelain` → empty (clean before review, after each mutation restore, and before verdict). `git merge-base --is-ancestor 91f2e712… 24f10657…` → 0 (base is ancestor; full range covered).
- `go run ./scripts/hermeticity/` → exit 0 (no output).
- `go vet ./pkg/worktree ./pkg/resources ./cmd/herd` → exit 0, no output.
- `GOMAXPROCS=2 go test -race -count=1 -p=2 ./pkg/resources` → `ok github.com/Kampe/Herdforge/pkg/resources 27.624s`, exit 0.
- `GOMAXPROCS=2 go test -race -count=1 -p=2 ./pkg/worktree` → `ok github.com/Kampe/Herdforge/pkg/worktree 80.969s`, exit 0 (includes all GC-safety, idle-discovery, manifest/journal, fairness, concurrency, cancellation, and real-lsof default-census tests).
- `GOMAXPROCS=2 go test -race -count=1 -p=2 -v -run 'IdlePool|ReclaimIdlePools' ./cmd/herd` → exit 0: `--- PASS: TestReclaimIdlePoolsOnPulse_ReclaimsCleanRootAndLeavesLeasedAlone (6.58s)`, `--- PASS: TestReclaimIdlePoolsOnPulse_GenuineFailurePropagatesNonzero (0.23s)`, `--- PASS: TestRunIdlePool_RealCLIDispatch (11.40s)`, `ok github.com/Kampe/Herdforge/cmd/herd 19.618s`.
- RED/GREEN guard A: mutate `lsofPositiveExitOne` to always false → `GOMAXPROCS=2 go test -race -count=1 -p=2 -run 'TestLSOFProcessInspectorPreservesPositiveExitOneOwnerEvidence' ./pkg/resources` → `--- FAIL ... git_census_test.go:158: positive lsof exit 1 should remain usable owner evidence: exit status 1`, `FAIL`, exit 1 → restore from sha256-verified backup (c007368c812345aa4cc31dce57d4d2aca40e3b4723428c1dc08a5109ee82b38a matched, diff clean) → same command → `ok ... 2.331s`, exit 0.
- RED/GREEN guard B: disable `usage.MetadataUnavailable` refusal in `inspectSlotUse` → `GOMAXPROCS=2 go test -race -count=1 -p=2 -run 'TestPoolGCRefusesUnknownProcessCensus' ./pkg/worktree` → `--- FAIL ... pool_gc_safety_test.go:416: GC error = <nil>, want fail-closed census refusal`, `FAIL`, exit 1 → restore from sha256-verified backup (6f2cbdbc45f47605c3bd87a5df3564220e9c9e79e09acd2c5d1aa7cb854d1830 matched, diff clean) → same command plus `TestPoolGCDefaultCensusRefusesLiveProcessWithOwnerEvidence|TestDiscoverIdlePools_CanceledContextIsRespectedNotSilentlyIgnored` → `ok ... 4.483s`, exit 0.
- Mutation scratch files removed; `git status --porcelain` empty at verdict time.

Limitations: no full-suite run, no `make lint all`, no Linux gate, no daemon execution, no live cleanup, no two-machine fixture, no provider/board writes — per packet scope (root owns publish/Linux gate/install/cleanup and the pending main governor integration). Supplementary prior reports (Google 7-finding pass, resolved-5be6 reports 2435/2436, 8f869 report 2428, final report 2437) are context, not formal PASS evidence for this verdict; every load-bearing claim above was re-executed or independently verified here at the exact pin.
