sha: 3103a2955df35d57717b23b933a79d3a7a8dc3e3
branch: fac-644-canonical-review-root
task: FAC-644
reviewer: w4
reviewer-family: google
builder-family: openai
verdict: PASS
reviewed-head: 3103a2955df35d57717b23b933a79d3a7a8dc3e3
---

The candidate successfully binds `verdict harvest` and ingest to one canonical project root using `gitroot.ProjectRoot()`.

The implementation in `cmd/herd/verdictharvest.go` properly checks for errors from `gitroot.ProjectRoot` and correctly resolves the project root, using it as the Git directory in subsequent commands and using `filepath.Join(root, path)` when necessary.

The test `TestVerdictHarvestUsesCanonicalProjectRootForFetchStatAndWrite` in `cmd/herd/verdictharvest_test.go` has been validated. It fails against the parent commit `HEAD^`, proving its non-vacuity (by attempting to run `verdict-harvest` and receiving a `fatal: not a git repository` because the cwd lacked a `.git` dir). When run against the candidate commit, the test passes successfully, demonstrating that the code now correctly locates and relies upon the canonical project root.

The supplemental refactoring in `pkg/launch/receipts_read.go` and `pkg/reviewledger/path.go` cleanly extract the redundant resolution logic into a `PathFor` function. All related module tests (e.g., `cmd/herd`) run hermetically and pass successfully. The isolated test environment constraints are properly respected.
