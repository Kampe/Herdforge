sha: 35ad0e2f65950b59ae1c4818331fb2a9d5fdd0c8
branch: repair/fac-765-authenticated-host-ingest
task: FAC-765
reviewer: review-fac-765-35ad0e2f6595
reviewer-family: openai
builder-family: xai
verdict: FAIL
reviewed-head: 35ad0e2f65950b59ae1c4818331fb2a9d5fdd0c8
---

Reviewed the complete fourteen-commit production range
9737258c9ecd7a48da215ea729cceff3cbc1d011..35ad0e2f65950b59ae1c4818331fb2a9d5fdd0c8 as one
R3 review. HEAD and ancestry are exact, the candidate source is clean, and the graph was
queried before source inspection (224 nodes, 2,089 edges; 39 changed files; risk 0.60).

### Findings

1. [Blocker] `cmd/herd/legacy_review_authority.go:66-80` collapses host-labelled harvest-queue
   events to one state per SHA. The native producer at `pkg/reviewledger/operations.go:522-527`
   emits a SHA-level `revoked` row for a host-A FAIL/BLOCKED and a later SHA-level `enqueue`
   row for a host-B PASS retry. `AdmittedPass` then sees only the final `queued` state and
   returns the PASS at lines 83-117 without checking same-host retry authority or any
   unsuperseded cross-host dissent. Thus a real cross-host veto can authorize the legacy
   closure when the append order is FAIL/BLOCKED then PASS; the reverse order happens to
   refuse, making the decision order-dependent. This violates the required cross-host
   dissent invariant and permits closure on a candidate that still has a native veto.

2. [High] `cmd/herd/candidate_cmd.go:80-95` treats every PASS row with `RetryOf` as an
   authenticated supersession without requiring EventRecord rows for both the current PASS
   projection and the named same-host prior reviewer. A fabricated PASS can therefore remove
   a real FAIL/BLOCKED from the `herd candidate` admitted-review projection, and that
   projection feeds `pkg/candidate.Resolve`, which can report `DispositionHarvest`. The
   candidate regression at `cmd/herd/candidate_cmd_test.go:75-95` constructs only verdict and
   synthetic supersession rows, so it does not exercise the native record-backed producer
   guard required by FAC-765.

### Tests run

- `code-review-graph status` and `detect-changes --base 9737258c9ecd7a48da215ea729cceff3cbc1d011 --brief`.
- Focused native regression set passed:
  `go test ./pkg/launch ./pkg/reviewledger ./pkg/review ./pkg/mergeadmit ./cmd/herd -run 'FAC-765|Retry|Veto|Admission|Harvest|Candidate|HostIngest|CompleteAdmission|BindEvidence|Canonical|Launch|Receipt' -count=1`.
- The changed-package broad command failed only on the packet-noted historical hook-timeout tests in `pkg/launch` (`TestOrdinaryRequestCannotBypassProductionDiscovery`, `TestClaudeCommandIncidentRequiresBoundHealthPolicyBeforeEffects`, `TestValidateOptionalHookWarningIsDeduplicatedAndIdentityPreserved`); `pkg/reviewledger`, `pkg/review`, `pkg/mergeadmit`, and `cmd/herd` completed successfully in that run.
- Targeted closure/retry tests passed, including `TestSameHostRetryReachesLegacyClosure`, `TestNativeRevocationWithdrawsLegacyClosure`, `TestLedgerReviewsSameHostRetryRetainsPass`, `TestFabricatedRetryCannotClearVeto`, `TestSameHostRetryReachesEveryFinalConsumer`, and `TestCrossHostRetryOrderPreservesVetoInBothAppendOrders`.
- Non-vacuity guard check: removing the current reviewer-record guard made `TestFabricatedRetryCannotClearVeto` fail (`native retry PASS without reviewer-b record was accepted`); restoring the exact candidate made it pass again.
- `git merge-base --is-ancestor` and `git diff --check` passed for the pinned range; candidate source diff is empty.

verification_digest: sha256:7e8c08a7292a36b101fc25035bf411ab4f0e6072ef43b14edcbd80f3fc328127
residual_risk: The focused core retry/admission projections are green, but legacy closure and candidate identity consumers still bypass the shared authenticated retry authority. The broad package run also retains the packet-noted hook-timeout environment failures.
reviewed_at: 2026-09-08T14:59:39Z
