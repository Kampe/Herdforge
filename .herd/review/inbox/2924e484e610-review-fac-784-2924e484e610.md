sha: 2924e484e61005fad6b79ab202313a158855c923
branch: recovery/fac-784-native-list
task: FAC-784
reviewer: review-fac-784-2924e484e610
reviewer-family: openai
builder-family: google
verdict: PASS
reviewed-base: 34302e89fe6f70157d9550b84f9e36ac41d1749c
reviewed-head: 2924e484e61005fad6b79ab202313a158855c923
---

Reviewed both commits in the declared full range as one unit. The native HTTP
Kaneo ListTasks implementation matches the deployed route and response shape:
`/api/task/tasks/{projectId}` with encoded `status`, `limit`, and `page` query
parameters, decoding `data.columns[*].tasks` and pagination metadata.

The implementation rejects structured error bodies under HTTP 200, missing or
mismatched board/task identity, malformed pagination, duplicate non-empty page
loops, failed later pages, and accumulated-count mismatches without returning
partial results. It permits a legitimate empty board, preserves status
filtering and deterministic page/column order, and leaves CLI behavior
unchanged. The prior admitted legacy-route mutation was independently watched
RED and the restored native implementation was watched GREEN.

No live provider was contacted and no credentials were read; all provider
checks used local fixtures. The review surface is clean at the exact candidate
SHA.

## Tests run

- `git diff --check 34302e89fe6f70157d9550b84f9e36ac41d1749c..HEAD` — exit 0
- `go test ./pkg/provider -run 'TestKaneoProvider_ListTasks|TestKaneoProvider_ListTaskLabelsBulk' -count=1` — exit 0
- `go test -race ./pkg/provider -run 'TestKaneoProvider_ListTasks|TestKaneoProvider_ListTaskLabelsBulk' -count=1` — exit 0
- Temporarily replaced the native endpoint with the admitted legacy `/api/task?projectId=...` route, then ran `go test ./pkg/provider -run '^TestKaneoProvider_ListTasks$' -count=1` — exit 1 (expected RED: handler observed `/api/task`, then HTTP 404)
- Restored the candidate endpoint with `kaneoListTasksURL` and reran the focused ListTasks tests — exit 0
- `go test ./pkg/provider -count=1` — exit 0
- `go test -race ./pkg/provider -count=1` — exit 0
- `go vet ./pkg/provider` — exit 0
- `go run ./scripts/hermeticity/` — exit 0

Residual risk: the review used fixtures only, so live Kaneo deployment behavior
beyond the supplied deployed-authority response evidence was not exercised.

reviewed_at: 2026-09-09T10:55:00-05:00
