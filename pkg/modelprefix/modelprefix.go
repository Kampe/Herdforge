// Package modelprefix owns model spellings shared by routing and quota
// acquisition. Keeping these protocol identifiers in one place prevents a
// routing alias from drifting away from its billing authority.
package modelprefix

const LiteLLMOllama = "litellm/ollama/"
